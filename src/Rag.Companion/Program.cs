// Placeholder entry point for the protocol-foundation work unit. The full
// `run --config <path>` orchestration arrives in a later work unit (Host/Program.cs).
// Until then this executable only proves the project restores and builds on win-x64.
return 0;
