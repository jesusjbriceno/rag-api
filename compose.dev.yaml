services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: api
    pull_policy: build
    ports:
      - 127.0.0.1:${RAG_API_PORT:-8080}:8080

  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: operator
    pull_policy: build
