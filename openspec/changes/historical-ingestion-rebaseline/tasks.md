# Tasks: Historical Ingestion Rebaseline

Implementation tasks for change `historical-ingestion-rebaseline` derived from `proposal.md`, `spec.md`, and `design.md`. The change is evidence-gated: every executable task follows strict TDD (RED → GREEN → TRIANGULATE → REFACTOR), and every tranche of tasks is blocked by the previous tranche's recorded evidence. Single-threaded writes; one work unit at a time; tests stay with the behaviour they verify; no task authorizes deployment, production corpus loading, push, commit, publish, branch/PR/CI creation, or RDD enablement — implementation-only verification (`dotnet test`, SQLite integration tests, Windows filesystem tests, Cloudflare/JWT unit tests, view-model/automation tests for the WPF shell) is the verification bound.

Protected WIP paths are explicit and unchanged throughout every work unit below: `src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, the AdminApp `Dockerfile` `admin` target, `compose.coolify.yaml` admin-related work, AdminApp CI workflows, and `src/Rag.Companion/` are read-only references until a Companion-disposition record explicitly authorizes a bounded follow-up. The existing `POST /api/v1/collections/{id}/ingestions:txt` real-time route stays compatible and untouched. PostgreSQL, llama.cpp, content storage, and administrative services stay non-public; no new internal-service endpoint is exposed.

The design keeps WPF as a **provisional** Windows shell; Electron is not selected and is not a precondition. The UI technology may be reconsidered after Gate L only with evidence (accessibility, packaging, operator feedback) — never as a Gate L blocker. No full-corpus target is chosen; the >500 GB corpus is not loaded.

**Historical evidence — prior blocked attempt:** `apply-progress.md` records the original Unit 1 inventory pilot attempt (single unit, ~1,050 minimum authored lines, edit surface limited to Core/Engine + tests) that was blocked by two verified blockers: `Microsoft.Data.Sqlite` not declared under Central Package Management (`Directory.Packages.props`), and a coherent minimum estimate exceeding the 400-authored-line budget. That record is preserved verbatim and is **not modified** by this revision.

**Division of the inventory pilot:** The original single Unit 1 is replaced by three coherent sequential subunits. Unit 1a was completed at ~747 authored lines after the maintainer explicitly accepted a `size:exception` for that unit alone; Units 1b and 1c retain their ≤400-line bounds. Unit 1b does not start before Unit 1a is green; Unit 1c does not start before both Unit 1a and Unit 1b are green; Units 2–14 do not start before Unit 1c is green.

## Review Workload Forecast

| Field | Value |
| ------- | ------- |
| Estimated changed lines | ~3,800–5,600 previously forecast for Units 1a–14; **~5,500–8,150 all-in** once Unit 11.prev is included (~1,700–2,550 authored lines across 11.prev-a, b1a, b1b, b2–b4, c–e). Does not include Unit 15, the future AdminApp change. Excludes generated artifacts, fixture key material, ephemeral backup files, and immutable ARM64/multi-platform image build outputs. |
| 400-line budget risk | **High** — Unit 1a was completed at ~747 lines under an explicitly accepted one-unit `size:exception`; Units 6, 8, 10, and 11 are individually forecast above the 400-line review budget; Unit 9 and Unit 7 are at the threshold. Units 1b and 1c retain their ≤400-line bounds. **Unit 11.prev adds a nine-slice surface (~1,700–2,550): 11.prev-d (~500–750) sits over budget and 11.prev-c/-e (~300–500) at the 400 threshold once tests travel with behaviour; the former 11.prev-b1 is refined into 11.prev-b1a (~190–260) and 11.prev-b1b (~270–380). The design amendment authorized no exception, so the required current-session `ask-on-risk` pause was taken along the chain: recorded slice-level decisions are `size:exception` for 11.prev-a (~560 measured), 11.prev-b1a (measured overrun of its 300-line attempt bound), 11.prev-b1b (overran; completed by the narrower evidence attempt) and 11.prev-b2 (~594 measured), plus an explicit 11.prev-b3 exception capped at 600 changed lines. 11.prev-d still requires its decision before its apply.** Even with strict work-unit commits, keeping tests with the behaviour they verify pushes several later units past 400. |
| Chained PRs recommended | **Yes** — split into autonomous, independently verifiable work units; each unit preserves a green and comprehensible repository state. Unit 11.prev is refined into nine dependency-ordered slices (a, b1a, b1b, b2–b4, c–e), each with its own tests and limited acceptance (the former b1 splits into b1a — schema v4 migration + backward-compatibility + backup evidence — and b1b — atomic command receipts/intents/audit mutation — so each slice is independently reviewable; measured actuals later exceeded sub-400 for several slices and the recorded decisions are in the delivery-decision record); its delivery (`size:exception` vs. approved re-slice) is gated by the current-session `ask-on-risk` pause; recorded decisions (11.prev-a, b1a, b1b and b2 exceptions plus the 11.prev-b3 600-line ceiling) are in the delivery-decision record, and 11.prev-d still requires its decision before its apply. The confirmed chain strategy is recorded below. |
| Suggested split | PR 1 (Unit 1a — central package + minimal Core + SQLite baseline, ~280–380 lines) → PR 2 (Unit 1b — bounded filesystem enumeration + candidate/error persistence, ~200–300 lines) → PR 3 (Unit 1c — atomic export + Engine `inventory` host + solution registration + integration tests, ~320–400 lines) → PR 4 (Unit 2 — sample selection, ~150–250) → PR 5 (Unit 3 — extraction benchmark probe, ~200–300) → PR 6 (Unit 4 — server timing evidence, ~150–250) → PR 7 (Unit 5 — Companion disposition record + extractor interface, ~100–200) → PR 8 (Unit 6 — SQLite engine with fakes, ~700–1000, `size:exception`) → PR 9 (Unit 7 — additive service-client grants + scoped tokens, ~300–450) → PR 10 (Unit 8 — historical API endpoints + migration, ~600–900, `size:exception`) → PR 11 (Unit 9 — engine API client + WinCred + Cloudflare headers, ~400–550, likely `size:exception`) → PR 12 (Unit 10 — bounded pipeline + selected extractor, ~500–700, `size:exception`) → PR 13 (Unit 11.prev-a — portable control contract, ~250–400 forecast; ~560 measured — `size:exception` authorized) → PR 14 (Unit 11.prev-b1a — schema v4 migration + backward-compatibility/backup evidence, ~190–260 forecast; measured overrun — `size:exception` authorized) → PR 15 (Unit 11.prev-b1b — atomic command receipts/intents/audit mutation, ~270–380 forecast; overran the attempt budget — `size:exception` authorized; completed via the narrower evidence attempt) → PR 16 (Unit 11.prev-b2 — coherent snapshot + inventory projection + privacy sentinel, ~230–330 forecast; ~594 measured — `size:exception` authorized) → PR 17 (Unit 11.prev-b3 — document keyset pages + event pages/cursors/resync, ~400–560 forecast; `size:exception` authorized with a 600-changed-line ceiling) → PR 18 (Unit 11.prev-b4 — restart durability + regression net, ~150–250) → PR 19 (Unit 11.prev-c — control codec + dispatcher, ~300–450) → PR 20 (Unit 11.prev-d — control service + supervised `serve` host, ~500–750, explicit `size:exception` or approved re-slice required) → PR 21 (Unit 11.prev-e — Windows named-pipe transport + pipe security evidence, ~300–450) → PR 22 (Unit 11 — WPF shell, ~400–600, likely `size:exception`) → PR 23 (Unit 12 — Gate L proof/evidence pack, ~100–200) → PR 24 (Unit 13 — Gate A OCI/Dokploy ARM64 validation, ~100–300) → PR 25 (Unit 14 — Gate C Coolify validation + full-corpus decision packet, ~100–300). |
| Delivery strategy | `ask-on-risk` (current session; required by the design amendment). Units forecast to exceed the 400-line budget surface as **explicit decisions** (not hidden) and require `size:exception` because the smallest honest work unit cannot be sliced smaller without separating tests from behaviour (work-unit-commits rule). Already-recorded exceptions apply only to their recorded units and are **not renewed**; Unit 11.prev's decisions were taken at the recorded pause: `size:exception` for 11.prev-a (~560 measured), 11.prev-b1a (measured overrun of its 300-line attempt bound), 11.prev-b1b (overran; completed by the narrower evidence attempt) and 11.prev-b2 (~594 measured), plus an explicit `size:exception` for 11.prev-b3 capped at 600 changed lines. Outstanding: 11.prev-d (explicit `size:exception` or an approved finer re-slice — natural candidate: 11.prev-d1 service core + projections + privacy; 11.prev-d2 supervision/lock/`serve`/drain + recovery) before its apply, with 11.prev-c/-e threshold-flagged; do not silently compress the work unit. |
| Chain strategy | **`feature-branch-chain` — confirmed.** Each unit lands as its own PR off the previous unit's branch; only the final integrator branch reaches `main`. This preserves maximum rollback isolation. The choice does not change evidence requirements or block gates. Unit 1a is the **first** authorized implementation unit; the chain begins with it. Unit 11.prev continues the chain as slices 11.prev-a, 11.prev-b1a–b4 and 11.prev-c–e (PR 13–21); the pending delivery decision does not reopen the strategy unless the user changes it. |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High
```

### Forecast per work unit (logical changed-line bands)

| Unit | Deliverable | Forecast band | Budget risk | Decision required |
| --- | --- | --- | --- | --- |
| **1a** | Central package declaration + minimal Core project + SQLite baseline (WAL/FK/FULL/single-writer/migration/backup/integrity) + data entities + focused persistence/classification tests | ~747 actual | **High** | **Completed under maintainer-accepted `size:exception`** |
| **1b** | Bounded filesystem enumeration + candidate/error persistence + reparse/inaccessible/path-escape behavior (consumes 1a) | ~200–300 | Low | No |
| **1c** | Atomic export (manifest.json/candidates.ndjson/summary.md + SHA-256) + Engine `inventory` host/local contract + solution registration + integration tests (consumes 1a/1b) | ~320–400 | Low–Medium | No (at threshold; cohesion rule binds tests to behaviour) |
| 2 | Deterministic stratified sample selection + frozen fingerprints | ~150–250 | Low | No |
| 3 | Extraction benchmark probe + sustained LibreOffice observations | ~200–300 | Low | No |
| **4** | Server-side safe timing evidence for embedding benchmark | **~483 actual** | **High** | **Yes — `size:exception` authorized in current session** |
| **5** | Companion disposition record + extractor interface | **~495 actual** | **High** | **Yes — `size:exception` authorized in current session** |
| 6 | SQLite lifecycle/checkpoint engine with fake extractor + fake API | ~700–1000 | **High** | **Yes — `size:exception` authorized in current session** |
| 7 | Additive service-client grants, scoped tokens, compatibility migration | ~300–450 | Medium | No |
| 8 | Historical reservation/stream/commit/status API + provenance + migration | ~600–900 | **High** | **Yes — `size:exception` requested** |
| 9 | Engine API client, Windows Credential Manager, Cloudflare headers, auth block | ~400–550 | Medium–High | **Yes — likely `size:exception`** |
| 10 | Bounded pipeline, persisted retries, watermarks, pause/resume, selected extractor | ~500–700 | **High** | **Yes — `size:exception` requested** |
| **11.prev-a** | Portable control contract (Contracts `net10.0`: versioned DTOs, state/error constants, `ILoaderControlClient` seam) + contract/portability tests + additive `Rag.sln` registration | **~560 actual** | **High** | **Yes — measured `size:exception` authorized in current session** |
| **11.prev-b1a** | Store slice: schema v4 migration + backup-before-migration/additive-only/backward-compatibility evidence + engine-instance row | ~190–260 forecast; **measured overrun of the 300-line attempt bound** | Medium–High | **Yes — `size:exception` authorized in current session** |
| **11.prev-b1b** | Store slice: atomic command commit for start/desired-state intents + durable receipt + operator audit row, replay/conflict/rejection semantics, typed precondition seam, acknowledgement-boundary fault injection | ~270–380 forecast; **overran the attempt budget; completed by the narrower evidence attempt** | Medium–High | **Yes — `size:exception` authorized in current session** |
| **11.prev-b2** | Store slice: coherent snapshot + candidate-derived inventory projection + privacy sentinel | **~594 actual** (production 160 + tests 434) | **High** | **Yes — measured `size:exception` authorized in current session** |
| **11.prev-b3** | Store slice: document keyset pages + event pages/cursors/resync + retention/restore invalidation | ~400–560 forecast; **600-changed-line ceiling** | **High** | **Yes — explicit `size:exception` authorized with a 600-line ceiling (current session; slice not started)** |
| **11.prev-b4** | Store slice: restart durability + full-solution regression net | ~150–250 | Low | No (sub-400) |
| **11.prev-c** | Control codec + dispatcher (framing, limits, fail-closed validation) with fake-stream tests | ~300–450 | Medium | Likely — at/above 400 once tests travel with behaviour |
| **11.prev-d** | Control service + supervised `serve` host composing `HistoricalPipeline`; receipt replay, pause responsiveness, recovery, privacy sentinel scans | ~500–750 | **High** | **Yes — explicit `size:exception` or approved re-slice required; design authorizes none** |
| **11.prev-e** | Windows named-pipe transport + ACL/peer/remote security evidence + Linux `platform_not_supported` guard | ~300–450 | Medium | Likely — at/above 400 once tests travel with behaviour |
| 11 | Replaceable WPF operator shell (consumes the verified Unit 11.prev contract; blocked by the 11.prev full gate) | ~400–600 | Medium–High | **Yes — likely `size:exception` (not renewed by the 11.prev amendment)** |
| 12 | Windows local vertical proof + Gate L evidence pack | ~100–200 | Low | No |
| 13 | OCI/Dokploy ARM64 bounded validation + Gate A | ~100–300 | Low | No |
| 14 | Coolify target validation + Gate C + full-corpus decision packet | ~100–300 | Low | No |
| 15 (future, separate change) | AdminApp provisioning/revocation integration | N/A here | N/A | Out of this change |

`size:exception` is recorded for the smallest honest slice only; the chain does not shrink code, tests, docs, or audit evidence to meet the budget. Each over-budget unit ships its full work-unit: tests travel with the behaviour they verify. The inventory pilot is divided into 1a/1b/1c; the totals across the three subunits preserve the full behavior of the original Unit 1 (~1,050 lines minimum) without any single subunit crossing the 400-line budget. Unit 11.prev is likewise divided into nine sequential slices (a, b1a, b1b, b2–b4, c–e): the former 11.prev-b1 is split into b1a (schema v4 migration + backward-compatibility + backup evidence) and b1b (atomic command receipts/intents/audit mutation) without dropping any behaviour; measured actuals later exceeded the budget for several slices (see the forecast table and the delivery-decision record), so the honest surface is reported rather than compressed and each slice's delivery decision is surfaced under `ask-on-risk` at the pause.

## Pre-Apply Design Gate

Each numbered precondition below is recorded in `design.md` (Evidence boundary, Scope and protected boundaries, Technology decisions, Idempotency/versions/deduplication, Concurrency/backpressure, Retry/failure policy, Audit/privacy, Direct loader-to-API contract, Cloudflare and internal-service boundary, Legacy collection and provenance, Observability, Companion decision gate, Validation gates, AdminApp disposition, Open decisions with required evidence). Tasks below do not invent values for any of them; each task names the evidence it consumes and the evidence it produces.

- [x] Evidence boundary recorded: TXT route shape unchanged, 1 MiB real-time cap preserved, JWT lifetime 15 min, OperationWorker concurrency not changed without evidence, Companion adapters treated as evidence/reference only.
- [x] Protected paths locked: `src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, AdminApp Docker/Compose/CI, `src/Rag.Companion/` are not modified by any task below until Companion disposition authorizes it.
- [x] Local model + audit/privacy contracts recorded: SQLite WAL, foreign keys, `synchronous=FULL`, single writer, versioned migrations with backup, append-only audit, allowlisted fields, no content/path/secret in audit.
- [x] Authentication layered contract recorded: Cloudflare service-token + RAG service-client → 15-min JWT with normalized scope and collection-grant identity/version.
- [x] Validation gates recorded: Gate L (local proof) → Gate A (OCI/Dokploy ARM64) → Gate C (Coolify). Full-corpus and production deployment are a separate decision after all gates.
- [x] Companion decision gate recorded: reuse/adapt/reference decision cites benchmark observation IDs and limitations; no production engine references Companion before the gate.
- [x] WPF provisional decision recorded: WPF selected for the proof behind the engine contract; Electron is not selected; UI technology may be revisited after Gate L with evidence, never as a Gate L blocker.
- [x] No full-corpus target chosen: 500 GB is the corpus lower bound; no capacity promise, no GPU/external-embedding authorization, no automatic taxonomy, no production deployment is authorized by this change.

## Tranche 1 — Evidence (Units 1a/1b/1c, 2–5)

No unit in this tranche may begin before the previous unit is green and its acceptance evidence is recorded. Tranche 1 produces no ingestion, no API change, no Companion change, no AdminApp change, no full-corpus run. Every unit is local-only; no environment gate is entered here.

### Inventory pilot — three coherent sequential subunits (1a, 1b, 1c)

The inventory pilot replaces former Unit 1. It is strictly read-only: it scans configured Windows roots, persists a canonical manifest, exports structured artifacts, and produces a human summary. No document content is opened. No API call. No Companion import. No UI. No environment gate. Tests stay with the behaviour they verify (work-unit-commits rule). The three subunits are sequential: 1b depends on 1a; 1c depends on both 1a and 1b; Units 2–14 do not start before 1c is green. **Unit 1a is the first authorized implementation unit for this change.**

#### Unit 1a — Central package declaration + minimal Core project + SQLite baseline + data entities

**Allowed edit surface (strict):** `Directory.Packages.props` (additive `PackageVersion` for `Microsoft.Data.Sqlite` only), `src/Rag.HistoricalLoader.Core/**` (new project tree, scaffolding only — enumerator, classifier, and exporter arrive in 1b/1c), `tests/Rag.HistoricalLoader.UnitTests/**` (Core tests tree, persistence + classification tests only), `Rag.sln` (additive Core + unit-test project registration only), and `openspec/changes/historical-ingestion-rebaseline/apply-progress.md` (planning progress only). **Unit 1a must not touch** `src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, AdminApp `Dockerfile` `admin` target, `compose.coolify.yaml` admin-related work, AdminApp CI workflows, `src/Rag.Companion/`, or any existing real-time route/handler. The `src/Rag.HistoricalLoader.Engine/**` project tree, atomic exporter, and integration tests belong to 1b/1c and **must not be created in 1a**. The SQLite bootstrap, data entities, and migration scaffolding **do not** read document content; persistence tests use in-memory or temp-file fixtures only.

- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/Inventory/ManifestPersistenceTests.cs` asserting SQLite WAL mode, foreign keys, `synchronous=FULL`, single serialized writer, versioned migration with backup-before-migration, integrity-check failure blocks processing and offers restore/export (never silent rebuild), and that an incomplete scan is never represented as complete. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/Inventory/CandidateClassifierTests.cs` covering classification of supported formats (`doc`, `docx`, `md`, `pdf`, `txt`), unsupported extensions, size-policy failures, access errors, and reparse-point outcomes, each producing a stable eligibility code. The classifier's inputs/outputs are wired through the data entities from Unit 1a; the bounded filesystem walk belongs to Unit 1b and is exercised here through in-memory candidate records only. <!-- sdd-owner: implementation -->
- [x] GREEN: in `Directory.Packages.props`, additively declare a `PackageVersion` entry for `Microsoft.Data.Sqlite` (version consistent with .NET 10 / `Microsoft.EntityFrameworkCore 10.0.1`); create `src/Rag.HistoricalLoader.Core/Rag.HistoricalLoader.Core.csproj` (.NET 10 class library) referencing the central `PackageVersion`; implement the SQLite bootstrap (WAL, foreign keys, `synchronous=FULL`, single serialized writer, versioned migrations with backup-before-migration, integrity-check failure handling) and the `loader_installation`, `source_root`, `manifest`, `candidate`, and `audit_event` entities per the local data model; scaffold the `Configuration/HistoricalLoaderOptions.cs` namespace skeleton (options bound but unused by 1a logic). Additively register `Rag.HistoricalLoader.Core` and `Rag.HistoricalLoader.UnitTests` in `Rag.sln`. Verify `dotnet test tests/Rag.HistoricalLoader.UnitTests/Rag.HistoricalLoader.UnitTests.csproj` is green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 1a:** (a) `dotnet test tests/Rag.HistoricalLoader.UnitTests/Rag.HistoricalLoader.UnitTests.csproj` is green; (b) `Directory.Packages.props` diff is additive (one new `PackageVersion` for `Microsoft.Data.Sqlite`); (c) `Rag.sln` registration is additive-only; (d) protected paths unchanged (`src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, AdminApp Docker/Compose/CI, `src/Rag.Companion/` not modified); (e) `src/Rag.HistoricalLoader.Engine/**` and integration tests not yet created; (f) no document content is opened (in-memory/temp-fixture tests only). **Decision gate — Unit 1b does not start until (a)–(f) are recorded in `apply-progress.md`.** <!-- sdd-owner: implementation -->

#### Unit 1b — Bounded filesystem enumeration + candidate/error persistence

**Consumes Unit 1a.** Builds the root-confined bounded enumerator and the candidate/error persistence path; consumes the SQLite baseline, entities, and writer from Unit 1a. Does **not** add the Engine host (Unit 1c) or the atomic exporter (Unit 1c). **Allowed edit surface:** `src/Rag.HistoricalLoader.Core/Inventory/**`, `tests/Rag.HistoricalLoader.UnitTests/Inventory/InventoryEnumeratorTests.cs`, and `apply-progress.md` (planning progress only). No new projects; no `Rag.sln` changes; no `Directory.Packages.props` changes; no Engine tree; no integration tests; no WIP-protected paths.

- [x] RED: add failing xUnit tests in `tests/Rag.HistoricalLoader.UnitTests/Inventory/InventoryEnumeratorTests.cs` asserting that, given a fixture tree containing eligible, unsupported, inaccessible, reparse, and partial-enumeration outcomes, the enumerator records every candidate/error in small transactions through the single writer, never follows reparse points by default, rejects paths escaping configured roots, and never reads document content (verified by file-open interception or by absence of any read-only handle on payload bytes). <!-- sdd-owner: implementation -->
- [x] GREEN: create `src/Rag.HistoricalLoader.Core/Inventory/InventoryEnumerator.cs` plus supporting types (root-confined walker, reparse-point filter, path-escape check, in-memory fixture reader for tests). Persist every candidate/error through the Unit 1a single writer in small transactions; wire classifier outcomes from Unit 1a into persisted `candidate` records. The manifest remains in `scanning` state until Unit 1c marks it complete. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend `InventoryEnumeratorTests.cs` to cover reparse-point filtering on Windows junctions, long-path / Unicode / case-equivalent path normalization, locked/permission-denied entries producing eligibility codes (not exceptions), and partial-enumeration outcomes that leave the manifest inspectable but not selectable. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 1b:** (a) `InventoryEnumeratorTests.cs` is green; (b) manifest rows reconcile with fixture counts; (c) protected paths unchanged; (d) no Engine project tree created; (e) no atomic export artifacts produced (that work belongs to Unit 1c). **Decision gate — Unit 1c does not start until (a)–(e) are recorded in `apply-progress.md`.** <!-- sdd-owner: implementation -->

#### Unit 1c — Atomic export + Engine `inventory` host + solution registration + integration tests

**Consumes Units 1a and 1b.** Wires the atomic exporter (manifest.json / candidates.ndjson / summary.md with SHA-256 hashes), creates the Engine project with the `inventory` subcommand and the versioned local contract (`IHistoricalLoaderEngine`), registers all four projects (Core, Engine, UnitTests, IntegrationTests) in `Rag.sln`, and exercises the full pilot end-to-end against fixture trees. **Allowed edit surface:** `src/Rag.HistoricalLoader.Engine/**` (new project tree), `src/Rag.HistoricalLoader.Core/Inventory/ManifestExporter.cs` + supporting types, `tests/Rag.HistoricalLoader.UnitTests/Inventory/ManifestExporterTests.cs`, `tests/Rag.HistoricalLoader.IntegrationTests/InventoryEndToEndTests.cs`, additive `Rag.sln` registration for Engine and IntegrationTests, and `apply-progress.md` (planning progress only). No `Directory.Packages.props` changes; no WIP-protected paths.

- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/Inventory/ManifestExporterTests.cs` asserting that `manifest.json` (versioned index/summary), `candidates.ndjson` (streaming record stream), and `summary.md` (human) are atomically exported via temp-file fsync + atomic rename, with recorded SHA-256 hashes that reconcile against stored rows; partial export cannot satisfy completion semantics. <!-- sdd-owner: implementation -->
- [x] GREEN: create `src/Rag.HistoricalLoader.Core/Inventory/ManifestExporter.cs` (atomic export of `manifest.json` / `candidates.ndjson` / `summary.md` with SHA-256 hashes; manifest marked complete only after every root reaches a terminal scan outcome); create `src/Rag.HistoricalLoader.Engine/Rag.HistoricalLoader.Engine.csproj` (.NET 10 console host) with the `inventory` subcommand, named-pipe host stub (not opened by 1c), and the `IHistoricalLoaderEngine` boundary interface exposing `RunInventoryAsync` and emitting structured progress events. Additively register `Rag.HistoricalLoader.Engine` and `Rag.HistoricalLoader.IntegrationTests` in `Rag.sln`. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: add `tests/Rag.HistoricalLoader.IntegrationTests/InventoryEndToEndTests.cs` parameterized over a fixture tree with reparse points, junctions, long/Unicode/case-equivalent paths, locked/permission-denied entries, and partial-enumeration outcomes; assert manifest totals reconcile with stored rows, exports hash correctly, summary is human-readable, and no content handle was opened. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 1c (completes the inventory pilot):** (a) `dotnet test Rag.sln --configuration Release` is green; (b) the engine runs `inventory` on a representative fixture tree and produces `manifest.json`, `candidates.ndjson`, and `summary.md`; (c) hashes reconcile against stored rows; (d) no document content is opened (verified by fixture handle interception or by absence of read calls); (e) protected paths unchanged (`src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, AdminApp Docker/Compose/CI, `src/Rag.Companion/`); (f) no API/UI/Companion/AdminApp code added; (g) `Rag.sln` registration is additive-only. **Decision gate — Unit 2 does not start until (a)–(g) are recorded in `apply-progress.md`.** <!-- sdd-owner: implementation -->

### Unit 2 — Deterministic stratified sample selection

Reads the canonical manifest from Unit 1c (the inventory pilot's atomic-export step). Produces a frozen sample set with algorithm, seed, allocations, weights, and coverage limitations. No environment interaction.

- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/Sampling/StratifiedSampleSelectorTests.cs` asserting that selection stratifies by observed format and empirical size band, allocates the operator-approved budget proportionally with a recorded per-stratum minimum, uses a recorded PRNG algorithm and seed, freezes each candidate's metadata fingerprint, reports uncovered strata and limitations, and **fails the gate** when budget cannot cover every observed stratum (no implicit "representative" claim). <!-- sdd-owner: implementation -->
- [x] RED: add failing golden tests in `tests/Rag.HistoricalLoader.UnitTests/Sampling/SampleGoldenTests.cs` proving that a fixed manifest + fixed seed produces byte-stable selection; changing the manifest or seed changes selection deterministically. <!-- sdd-owner: implementation -->
- [x] GREEN: create `src/Rag.HistoricalLoader.Core/Sampling/StratifiedSampleSelector.cs`, `SampleAllocation.cs`, `StratumCoverage.cs`, and persist `sample_set` entities in SQLite with frozen fingerprints. Add a `--select-sample <manifest-id> --budget <n> --seed <s>` subcommand on the engine. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests to cover boundary/outlier strata, missing-size-band warnings, manifest-incompleteness rejection, and audit-event emission for the selection. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse selection record types into a single `Sampling/SampleSet.cs` namespace; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 2:** (a) golden selection reproducible from a fixed manifest/seed; (b) uncovered strata fail the gate; (c) changed candidates invalidate selection; (d) `apply-progress.md` records the operator-approved sample budget, confidence/coverage rules, and sustained-run duration **before** Unit 3 starts. **Decision gate — Unit 3 does not start until the predeclared sample parameters are recorded.** <!-- sdd-owner: implementation -->

### Unit 3 — Extraction benchmark probe and sustained observations

A thin probe that **may** invoke existing Companion adapters through a reflection or process boundary **without modifying `src/Rag.Companion/`**. Records extraction duration, normalized-text size, error/timeout/hang classifications, and process CPU/working set/disk-I/O counters. The probe is **not** a production dependency.

- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/Benchmark/ExtractionProbeTests.cs` asserting that the probe measures per-candidate discovery, snapshot, extraction, and staging durations; records resource counters with method, sampling interval, machine/OS/runtime version, adapter version, and configuration; classifies LibreOffice timeouts/hangs explicitly; and that **no `src/Rag.Companion/` file is referenced as a project dependency** (verified by `git diff --name-only` not touching Companion paths during apply). <!-- sdd-owner: implementation -->
- [x] RED: add failing tests asserting sustained-run criteria: probe runs for the predeclared duration (recorded in Unit 2), reports timeouts/hangs/error rates, and refuses to declare feasibility if the run does not satisfy predeclared reliability criteria. <!-- sdd-owner: implementation -->
- [x] GREEN: create `src/Rag.HistoricalLoader.Core/Benchmark/ExtractionProbe.cs`, `BenchmarkObservation.cs`, and a `benchmark extraction` subcommand on the engine. Persist observations in `benchmark_observation`. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests to record report composition: count, median, p95 where sample size supports it, observed range, docs/hour, source/normalized GB/hour, errors/timeouts/hangs, peak resources, queue growth, confidence/coverage limitations, and explicit assumptions for any full-corpus weighted range. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse probe configuration into `Benchmark/ProbeOptions.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 3:** (a) benchmark report persists with all required fields; (b) sustained-run observations recorded for the predeclared duration; (c) protected Companion path unchanged (apply diff does not touch `src/Rag.Companion/`); (d) `apply-progress.md` records the weighted full-corpus range with assumptions and uncertainty. **Decision gate — Unit 5 (Companion disposition) does not start until the benchmark report is reviewed.** <!-- sdd-owner: implementation -->
- [x] **Authorization record — Unit 3 `size:exception`:** User explicitly authorized a `size:exception` for Unit 3 in the current session. Reason: ≈950 authored lines versus the 400-line review budget and the ~200–300 forecast. Scope is limited to the local extraction benchmark probe and sustained-observation machinery only. Approved sample parameters: 30 candidates, minimum 1 per non-empty stratum, 95% confidence / ±10% margin, 2-hour sustained run. Exclusions: no `src/Rag.Companion/` modifications, no AdminApp WIP changes (`src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, AdminApp Docker/Compose/CI), no deployment, no full-corpus run, no RDD enablement. <!-- sdd-owner: parent -->

### Unit 4 — Server-side safe timing evidence for embedding benchmark

Bounded instrumentation that records chunk count, queue wait, chunking duration, embedding request count/duration, indexing duration, terminal state, active/pending historical counts, and real-time queue impact — **without changing real-time wire behavior** and without exposing internal metrics.

- [x] RED: add failing tests asserting that operation evidence surfaces chunk count, queue wait, chunking/embedding/indexing durations, embedding calls, terminal state, and real-time impact metrics; that the existing real-time ingestion contract tests remain green; and that no internal metric is exposed to the loader or the public surface. <!-- sdd-owner: implementation -->
- [x] GREEN: extend the operation worker with additive historical workload classification and operation telemetry; preserve current real-time worker concurrency. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with a bounded local sample producing chunk/queue/embedding timings; verify `dotnet test tests/Rag.IntegrationTests --filter "FullyQualifiedName~RealTime"` remains green. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse telemetry fields into a single `Operation/HistoricalTelemetry.cs` namespace; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 4:** (a) bounded local sample produces chunk/queue/embedding timings; (b) existing real-time tests green; (c) `apply-progress.md` records observed CPU/RAM/queue growth and real-time latency impact. **Decision gate — Unit 5 does not start until embedding capacity evidence is recorded.** <!-- sdd-owner: implementation -->
- [x] **Authorization record — Unit 4 `size:exception`:** User explicitly authorized a `size:exception` for Unit 4 in the current session. Reason: ≈483 non-blank authored lines versus the 400-line review budget and the ~150–250 forecast. Scope is limited to non-persistent internal server telemetry (chunk count, queue wait, chunking/embedding/indexing durations, embedding calls, terminal state, active/pending counts, real-time queue impact). Exclusions: no `src/Rag.Companion/` modifications, no AdminApp WIP changes (`src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, AdminApp Docker/Compose/CI), no API public-contract changes, no database migrations, no deployment, no full-corpus run, no RDD enablement. <!-- sdd-owner: parent -->

### Unit 5 — Companion disposition record and extractor interface

Records exactly one outcome (reuse, adapt, reference only) with observation/report IDs and limitations. Defines an `IExtractor` interface behind the engine contract so the UI is unaffected by the decision.

- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/Extraction/IExtractorContractTests.cs` asserting that `IExtractor` is the only abstraction the engine depends on, that the implementation is swappable at composition time, and that the WPF shell has no transitive reference to any Companion namespace (verified by assembly-level reference checks). <!-- sdd-owner: implementation -->
- [x] RED: add failing tests asserting that `apply-progress.md` Unit 5 carries the chosen disposition, the observation/report IDs, the limitations, and that the **engine project does not project-reference `Rag.Companion`** (verified by `dotnet list src/Rag.HistoricalLoader.Engine package reference --include-transitive` and by a build that does not consume Companion as a project reference). <!-- sdd-owner: implementation -->
- [x] GREEN: create `src/Rag.HistoricalLoader.Core/Extraction/IExtractor.cs`, `ExtractionRequest.cs`, `ExtractionResult.cs`, and a fake `FakeTextExtractor` used by Units 6 and 10. Persist `CompanionDisposition.md` evidence in `docs/historical-ingestion-rebaseline/companion-disposition.md`. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests to prove the engine can run inventory/benchmark/proof against `FakeTextExtractor` without any Companion reference. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse extractor request/result types into `Extraction/Model.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 5:** (a) `docs/historical-ingestion-rebaseline/companion-disposition.md` records exactly one disposition (reuse/adapt/reference) with cited observations; (b) `src/Rag.HistoricalLoader.Engine` does not project-reference `Rag.Companion`; (c) `apply-progress.md` records the disposition and any unresolved evidence. **Decision gate — Unit 6 does not start until the disposition record is approved.** A "reuse" or "adapt" disposition authorizes a bounded follow-up to `src/Rag.Companion/` that must be its own future change and its own review; "reference only" forbids any future Companion reference.
- [x] **Authorization record — Unit 5 `size:exception`:** User explicitly authorized a `size:exception` for Unit 5 in the current session. Reason: ≈495 authored lines versus the 400-line review budget and the ~100–200 forecast. Scope is limited to the `IExtractor` interface contract, `ExtractionRequest`/`ExtractionResult` model, `FakeTextExtractor`, `IExtractorContractTests`, and `CompanionDisposition.md` evidence. Disposition outcome: `adapt`. Exclusions: no `src/Rag.Companion/` modifications, no AdminApp WIP changes (`src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, AdminApp Docker/Compose/CI), no API public-contract changes, no database migrations, no deployment, no full-corpus run, no RDD enablement. <!-- sdd-owner: parent --> <!-- sdd-owner: implementation -->

## Tranche 2 — Local proof (Units 6–12)

Units 6–12 (including the Unit 11.prev headless control prerequisite between Units 10 and 11) implement the local engine, the historical API, the WPF shell, and produce the Gate L evidence pack. Every executable task continues strict TDD; tests stay with the behaviour they verify; the SQLite engine is verified first with fake extractor and fake API before any real I/O.

### Unit 6 — SQLite run/lifecycle/checkpoint engine (fake extractor + fake API)

The largest unit; tests + behaviour cannot be sliced smaller without violating the work-unit-commits rule. `size:exception` requested.

- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/Lifecycle/DocumentLifecycleStateMachineTests.cs` asserting the state transition table (`pending → snapshotting → extracting → staged → reserving → uploading → committing → remote_pending → loaded`; `snapshotting/extracting → skipped_document_error`; `reserving/uploading/committing/polling → retry_wait → same stage (≤3 dispatched attempts) → retry_exhausted_network`; recovery → `interrupted → last durable safe stage`; auth/capacity/contract → `blocked_auth`/`blocked_operator_action`); that `loaded`, `skipped_document_error`, and `retry_exhausted_network` are terminal; that every transition and audit event commit in one SQLite transaction before UI notification; that UI counters are projections from durable rows. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.IntegrationTests/Sqlite/LifecyclePersistenceTests.cs` asserting WAL recovery, transaction/audit atomicity, migration backup, corrupted/stale staging references, single-writer behavior, restart reconciliation that recovers from a forced kill at every durable boundary, and pause drain semantics. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/Retry/AttemptAccountingTests.cs` asserting each dispatched HTTP operation persists an attempt before dispatch, that three is the maximum per run/generation, that successful `pending` polls are not failed retries, that cancellation before dispatch does not consume an attempt, and that unknown outcome after dispatch consumes an attempt and reconciles via the idempotent resource. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests asserting that `Pause` commits `pause_requested`, stops claiming new documents, lets active work reach a durable safe boundary, and that `paused` is reported only when no local stage lacks a durable receipt. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests asserting terminal-state immutability: previously loaded, skipped, or retry-exhausted records cannot become pending automatically; restart never resets attempts. <!-- sdd-owner: implementation -->
- [x] GREEN: create `src/Rag.HistoricalLoader.Core/Lifecycle/DocumentLifecycle.cs`, `LifecycleStore.cs`, `AttemptCounter.cs`, `SqliteRunStore.cs` with versioned migrations and backup-before-migration, `Run` and `RunDocument` entities, bounded `Channel<T>` buffers fed by durable claims, and a `FakeHistoricalApiClient` implementing `IHistoricalApiClient` for state-machine tests. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: add fault-injection tests — kill at every durable boundary, then verify recovery from a clean restart; force a network-class retry sequence and assert the third attempt exhausts; force a document-class extraction failure and assert subsequent documents remain processable. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse lifecycle types into `Lifecycle/Model.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 6:** (a) state-machine + persistence + retry + pause/restart tests are green; (b) `apply-progress.md` records the durable-safe-boundary list and the kill/restart evidence table; (c) protected paths unchanged. **Decision gate — Unit 7 does not start until the three-attempt ceiling and pause/resume semantics are proven by tests.** <!-- sdd-owner: implementation -->
- [x] **Authorization record — Unit 6 `size:exception`:** User explicitly authorized a `size:exception` for Unit 6 in the current session. Reason: ~700–1000 authored lines versus the 400-line review budget and the ~700–1000 forecast. Scope is the SQLite run/lifecycle/checkpoint engine with `FakeTextExtractor` and `FakeHistoricalApiClient` only. Exclusions: no `src/Rag.Companion/` modifications, no AdminApp WIP changes (`src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, AdminApp Docker/Compose/CI), no API public-contract changes, no database migrations outside the loader SQLite store, no production/deployment, no full-corpus run, no production ingestion, no RDD enablement. <!-- sdd-owner: parent -->

### Unit 7 — Additive service-client grants, scoped tokens, compatibility migration

Extends `POST /api/v1/auth/token` additively with `scope`; preserves omitted-scope behavior; issues normalized `scope` and collection-grant version; rejects unknown/ungranted scope; returns `401` for absent/expired/revoked credentials; `403` for valid but insufficient tokens.

- [x] RED: add failing tests in `tests/Rag.UnitTests/Auth/TokenScopeExchangeTests.cs` asserting that `scope` is optional in the request, normalized in the response, omitted `scope` preserves current exchange, unknown/ungranted `scope` returns `invalid_scope`, expired/revoked credentials remain `401`, valid but insufficient tokens receive `403`, and that JWTs include client/credential IDs/version plus normalized `scope` and collection-grant identity/version. <!-- sdd-owner: implementation -->
- [x] RED: add failing compatibility tests asserting existing real-time clients continue to behave identically when no scope is requested, that real-time ingestion route tests remain green, and that historical clients receive only `historical:uploads.write` + `historical:operations.read` plus the `legacy` collection grant. <!-- sdd-owner: implementation -->
- [x] GREEN: extend `src/Rag.Application/Auth/TokenExchange.cs` (or equivalent handler) additively; update `JwtAuthentication.cs` to include scope and collection-grant identity/version claims; add `Rag.Application/Auth/HistoricalScopes.cs` with the canonical scope strings. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with rotation/revocation: provisioning a second active credential, updating the loader entry, verifying token exchange, then revoking the old credential and confirming old JWTs are invalidated by credential version/status checks. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse scope constants into a single `Auth/HistoricalScopes.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 7:** (a) existing clients retain behavior; (b) loader token is denied outside `legacy` historical scopes; (c) expiry/rotation/revocation tests pass; (d) `apply-progress.md` records the compatibility grant and the migration; (e) the feature is gated behind `HistoricalIngestion:Enabled=false` by default. **Decision gate — Unit 8 does not start until existing real-time tests remain green and `HistoricalIngestion:Enabled=false` is the documented default.** <!-- sdd-owner: implementation -->

### Unit 8 — Historical reservation/stream/commit/status API + provenance + migration

Additive route family; `POST /api/v1/historical/collections/{collectionId}/uploads`, `PUT /api/v1/historical/uploads/{uploadId}/content`, `POST /api/v1/historical/uploads/{uploadId}:commit`, `GET /api/v1/historical/uploads/{uploadId}`, `GET /api/v1/historical/collections/{collectionId}/operations/{operationId}`. `size:exception` requested.

- [x] RED: add failing integration tests in `tests/Rag.IntegrationTests/Historical/HistoricalUploadApiTests.cs` proving: idempotent reserve with source key/candidate/manifest/run IDs/safe provenance/normalized-text SHA-256/declared bytes/idempotency key returns upload ID/state/accepted limits/correlation; streamed `PUT` verifies declared length and digest, atomically publishes internally, and is idempotent for repeated matching upload; `commit` idempotently creates/finds source document and version, persists provenance, enqueues a historical operation; `GET` reconciles ambiguous outcomes without repeating content mutation; `GET .../operations/{operationId}` returns safe state, failure stage/code, queue wait, chunk count, and stage timings. The historical normalized-text size cap is an explicit configuration; tests prove request rate, declared length, observed length, content hash, abandoned-upload expiry, per-client pending quota, and total storage watermark enforcement. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests proving idempotency-fingerprint conflict returns `409 Conflict` without mutating the existing record, that changed normalized content for the same source creates a new document version, that equal normalized content from two different source-document keys remains two documents, and that the server verifies declared length and SHA-256 while streaming before commit. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests proving historical operations are workload-classified separately from real-time, that pending-quota rejections return `429 Retry-After`, that real-time ingestion is not starved (existing real-time tests remain green), and that the API contract carries safe operation telemetry (chunk count, queue wait, chunking/embedding/indexing durations, embedding calls, terminal state) without exposing internal service addresses. <!-- sdd-owner: implementation -->
- [x] GREEN: add `src/Rag.Api/Historical/HistoricalUploadEndpoints.cs`, `HistoricalUploadHandler.cs`, `HistoricalOperationEndpoints.cs`, `HistoricalOperationHandler.cs`, with additive migration in `src/Rag.Infrastructure/Migrations` for historical upload/version/operation/provenance tables. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with cross-source provenance (equal content, different source keys → two documents); admission fairness (real-time first during contention); and a quota-rejection scenario that records the rejection without starving real-time. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse historical endpoint registrations into a single `src/Rag.Api/Historical/HistoricalEndpointExtensions.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 8:** (a) streaming/idempotency/versioning/provenance/quota/fairness/telemetry tests are green; (b) `apply-progress.md` records the additive migration and the contract tests for streaming limits; (c) `HistoricalIngestion:Enabled=false` remains the default; (d) no internal service address is exposed. **Decision gate — Unit 9 does not start until real-time fairness and quota behavior are proven by tests.** <!-- sdd-owner: implementation -->

### Unit 9 — Engine API client, Windows Credential Manager, Cloudflare headers, auth block

Adds the engine-side API client, WinCred integration (separate entries for RAG service credential and Cloudflare service token; short-lived tokens memory-only), Cloudflare header boundary, and global auth-block behavior. Likely `size:exception`.

- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/ApiClient/HistoricalApiClientTests.cs` proving correct request shape (idempotency key, source key, declared bytes, SHA-256), response handling, and that unknown outcome after dispatch consumes an attempt and reconciles via the idempotent resource. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests proving reusable RAG service credentials and Cloudflare service tokens are stored in separate Windows Credential Manager entries, that short-lived API tokens are memory-only and reacquired after restart, and that audit/log/report surfaces are scanned for sentinel values for client secret, Cloudflare secret, bearer token, content, and absolute path — none may appear. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests proving auth failures pause submissions globally (no document-level retry loop), that a Cloudflare denial and an API `401` are both classified as `blocked_auth`, that expired/revoked/insufficient-scope returns are correctly distinguished, and that ambiguous mutations reconcile before another mutation is attempted. <!-- sdd-owner: implementation -->
- [x] GREEN: create `src/Rag.HistoricalLoader.Engine/Api/HistoricalApiClient.cs` (extends Unit 6's fake), `Security/WindowsCredentialManager.cs`, `Security/CloudflareHeaderBuilder.cs`, `Security/TokenMemoryCache.cs`. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with token expiry mid-run (acquire fresh token and resume without consuming attempts), credential rotation (provision new, verify, revoke old, confirm new token still works and old is rejected), and ambiguous mutation reconciliation (server commit succeeded but response lost → replay via idempotency key → no duplicate). <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse security types into `Security/` namespace; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 9:** (a) sentinel privacy scans pass; (b) token restart/expiry tests pass; (c) ambiguous-mutation reconciliation tests pass; (d) Cloudflare vs. application denial classified correctly; (e) `apply-progress.md` records the rotation procedure and the audit-redaction evidence. **Decision gate — Unit 10 does not start until WinCred isolation and privacy sentinel scans are green.** <!-- sdd-owner: implementation -->

### Unit 10 — Bounded pipeline, persisted retries, staged watermarks, pause/resume, selected extractor

Wires the engine to the real extractor (chosen by Unit 5), the real API client (Unit 9), and the SQLite lifecycle (Unit 6). Starts at concurrency 1; changes only during recorded benchmark sweeps. `size:exception` requested.

- [x] RED: add failing tests proving: the three-attempt ceiling holds across a forced restart (a third dispatched attempt that crashes before completion is reconciled by idempotency and the next dispatch counts as a new attempt within the same generation, never a fourth); document faults do not block later documents; bounded queues stop extraction at the configured staged-byte/count watermarks; pause/resume round-trips with the engine host process killed and restarted; no unconfirmed `loaded` state is ever emitted. <!-- sdd-owner: implementation -->
- [x] RED: add failing tests asserting the engine never consumes a fourth attempt for any HTTP operation, that cancellation before dispatch does not consume an attempt, and that retry classification (transient network/boundary, auth/authz, document, contract/data, local capacity) is mapped per the design table. <!-- sdd-owner: implementation -->
- [x] GREEN: create `src/Rag.HistoricalLoader.Engine/Pipeline/HistoricalPipeline.cs`, `WatermarkScheduler.cs`, `PauseController.cs`, `ResumeController.cs`, and the selected `IExtractor` adapter (chosen by Unit 5). Persist run configuration snapshot without secrets. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: add fault-injection tests — transient sequence (timeout, 429, 502, 503, 504) with full-jitter backoff; document extraction failure followed by a later eligible document; staging disk watermark hit; SQLite failure → checkpoint and block; Cloudflare denial vs. application `401` → `blocked_auth`. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse pipeline types into `Pipeline/Model.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — Unit 10:** (a) three-attempt ceiling proven across restart; (b) document skip continuity proven; (c) bounded queues proven; (d) pause/resume proven; (e) `apply-progress.md` records the watermark values chosen from inventory/operator disk budget and the concurrency value (1). **Decision gate — Unit 11.prev does not start until the bounded pipeline is green against the real extractor and the real API client (still loopback-only). Unit 11 (the shell) is additionally blocked by the Unit 11.prev full prerequisite gate below and no longer depends directly on Unit 10.** <!-- sdd-owner: implementation -->

### Unit 11.prev — Headless local control prerequisite

`design.md` → "Unit 11.prev — Headless local control prerequisite" is the binding source for this unit. It delivers the engine-side control service and the **version-1 local control contract over a current-user Windows named pipe** — not a desktop shell and not WPF. It supersedes the exploration recommendation to begin with Desktop-local contract DTOs: the transport-neutral contract is settled first so the later shell cannot invent an incompatible engine dependency. **Unit 11 acceptance remains blocked until every item of the full prerequisite gate below is verified**; 11.prev does not replace Windows operator or Gate L evidence, and no prior exploration/task assertion about exceptions or chain strategy is renewed.

**Ownership and dependency direction (binding):** the engine exclusively owns approved batch lookup, database/staging paths, extractor/API composition, credential access, pipeline lifetime, durable commands, and audit projections. The pipe dispatcher validates and delegates; it owns no ingestion policy. 11.prev consumes existing prepared batches — no batch preparation, inventory traversal commands, credential provisioning, or arbitrary configuration editing on the wire; inventory snapshots show persisted totals only. Internal `IHistoricalLoaderEngine` methods remain compatible; their path-bearing inputs are never exposed over IPC.

**Excluded from 11.prev (binding):** WPF/XAML, Desktop/view-model projects, Windows UI automation, Windows-targeted project references, CI workflow changes, deployment, real corpus runs, API/server migrations, and any change to `src/Rag.Companion/`, `src/Rag.AdminApp/`, `src/Rag.AdminApp.Host/`, AdminApp Docker/Compose/CI, or the existing real-time ingestion path.

**Portable/Linux CI constraints (binding):** core service/codec/contract tests stay `net10.0` and run on Linux with fake transport, extractor, API, and credential seams; the default `Rag.sln` restore/build/test must stay viable on Linux; only additive registration of the portable Contracts library and references is allowed; no `EnableWindowsTargeting`, `UseWPF`, Windows desktop runtime, or conditional omission of tests enters the default solution, and no CI workflow changes. Portable pipe/framing tests on Linux do **not** prove Windows ACL enforcement — Windows acceptance is separate evidence.

**Windows pipe security evidence (required before the production control boundary is declared verified):** local-machine/current-user only with owner-restricted ACLs; explicit rejection of remote clients; peer identity verification on both ends where needed; endpoint derived from a fixed prefix plus opaque installation/user identity (never source paths); no caller-selected pipe/database path over IPC; fail closed when ACL/identity enforcement is unavailable — never fall back to localhost TCP, a public pipe, or anonymous access. Same-user compromise is outside this isolation boundary. Windows acceptance must separately prove same-user connection success and foreign-user/remote denial; a Linux `platform_not_supported` guard must fire before touching Windows credentials or starting ingestion.

**Delivery slices and budget status:** 11.prev is delivered incrementally as **11.prev-a → 11.prev-b1a → 11.prev-b1b → 11.prev-b2 → 11.prev-b3 → 11.prev-b4 → 11.prev-c → 11.prev-d → 11.prev-e** (a refinement of the design's contract/service/transport decomposition); each slice contains its own tests and independently reports its limited acceptance, and each is blocked by the previous slice's recorded limited acceptance. The design amendment authorized no implementation or `size:exception` by itself; the required current-session `ask-on-risk` pause was then taken along the chain — 11.prev-a, b1a, b1b and b2 carry recorded `size:exception` decisions and 11.prev-b3 carries an explicit exception capped at 600 changed lines (see the delivery-decision record; 11.prev-d's decision remains outstanding). Rollout validates against prepared fixture batches and fakes first, then a bounded approved Windows/loopback run; the pipe opens only under explicit `serve`. Rollback stops the new host at a checkpoint, leaves existing CLI use available where schema-compatible, preserves the migrated database, staged files, audit, and receipts, and blocks binary downgrade when an older binary cannot read the schema (never silently restore a pre-run backup and lose newer receipts).

#### 11.prev-a — Portable control contract

**Allowed edit surface (strict):** new `src/Rag.HistoricalLoader.Contracts/**` (`net10.0`, no packages; no Engine/Core, SQLite, HTTP, credential, filesystem, or WPF dependency); additive `Rag.sln` registration for Contracts only; additive `ProjectReference` for Contracts in `tests/Rag.HistoricalLoader.UnitTests/Rag.HistoricalLoader.UnitTests.csproj`; new `tests/Rag.HistoricalLoader.UnitTests/Control/ControlContractTests.cs`; `apply-progress.md` (planning progress only). **Must not touch** Engine/Core sources, `NamedPipeHost.cs`, `Program.cs`, WPF/Desktop, CI workflows, or any protected path.

- [x] RED: add failing tests in `tests/Rag.HistoricalLoader.UnitTests/Control/ControlContractTests.cs` asserting the version-1 wire contract: every request carries `protocol_version: 1`, a UUID `request_id`, an allowlisted `operation`, and its typed payload; every response echoes version/request ID and carries `status` (`ok`/`accepted`/`rejected`) with a stable optional `error_code` and typed payload; UTC timestamps are ISO-8601; identifiers are opaque strings/UUIDs; states are explicit snake-case strings, never CLR enum ordinals; unknown versions/operations and malformed required fields fail closed before dispatch (`unsupported_version`); optional additive fields are ignorable; breaking field/state/semantic changes require a new protocol version; `hello` exposes supported versions, capabilities, installation ID, engine-instance ID (new each process), and transport limits, and must succeed before other operations; serialization never emits raw exception payloads. Add the reflection/assembly guard: the Contracts assembly has no Engine/Core/SQLite/HTTP/credential/filesystem/WPF dependency and `ILoaderControlClient` (hello, start, pause, resume, get_state, get_documents, get_events) is its only transport-neutral seam. <!-- sdd-owner: implementation -->
- [x] GREEN: create `src/Rag.HistoricalLoader.Contracts/Rag.HistoricalLoader.Contracts.csproj` with the protocol version/state/error constants, request/response envelopes, per-operation DTOs (`hello`, `start`, `pause`, `resume`, `get_state`, `get_documents`, `get_events`), and the `ILoaderControlClient` seam; additively register Contracts in `Rag.sln`; reference it from the unit-test project only (the Engine reference arrives in 11.prev-c). DTOs carry no content, absolute paths, source keys, credential material, or raw exception text. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with future-version negotiation (`hello` supported-versions payload; unsupported clients receive `unsupported_version`, never an implicit downgrade), duplicate/unknown operations rejected before dispatch, stable codes for `command_conflict`, `resync_required`, and `platform_not_supported`, and serialized-payload sentinel defaults proving no DTO field can carry content/paths/secrets. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse wire DTOs into `Contracts/Model.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — 11.prev-a (limited acceptance: contract slice only):** (a) contract/portability tests green on Linux `net10.0`; (b) `Rag.sln` registration additive-only; (c) no `EnableWindowsTargeting`/`UseWPF`/Windows-targeted reference; (d) Engine/Core/WPF/CI/protected paths unchanged. **Decision gate — 11.prev-b does not start until (a)–(d) are recorded in `apply-progress.md`.** <!-- sdd-owner: implementation -->

#### 11.prev-b — Loader store: command receipts + coherent projections (sub-sliced b1a–b4)

**Consumes the Unit 10 store baseline; sequenced strictly after 11.prev-a.** The store slice is delivered as five strictly sequential sub-slices, **11.prev-b1a → 11.prev-b1b → 11.prev-b2 → 11.prev-b3 → 11.prev-b4**; each sub-slice carries its own tests and its own limited acceptance recorded in `apply-progress.md`, and each is blocked by the previous sub-slice's recorded acceptance. The former 11.prev-b1 is split into **11.prev-b1a** (schema v4 migration + backward-compatibility + backup evidence) and **11.prev-b1b** (atomic command receipts/intents/audit mutation) so that the migration/backup surface and the command-commit surface each land sub-400; the split adds no behaviour and removes none — the combined honest surface is unchanged. Tests stay with the behaviour they verify; the honest surface is reported, not compressed. Core stays independent of Contracts (protocol-to-store mapping belongs to the service slice). No new projects; no Engine/Contracts/WPF/CI changes; no protected paths; `SqliteRunStore` and the existing Unit 6/10 lifecycle, attempt-accounting, and audit semantics are not modified.

**Store baseline consumed (facts, unchanged by this unit):** `SqliteStore.CurrentSchemaVersion = 3`; the single-writer `SemaphoreSlim` (internal, no `InternalsVisibleTo`); the `PRAGMA quick_check` integrity gate; WAL / foreign keys / `synchronous=FULL`; `SqliteRunStore` per-document commit + one `audit_event`; `audit_event` already an installation-scoped durable monotonic cursor; `manifest`/`candidate` inventory facts; the 11.prev-a contract vocabulary. The migration reuses the established SQLite backup process (`BackupDatabase`); never a raw file copy while the database is active. Cross-slice obligations recorded here, owned elsewhere: the store↔wire vocabulary agreement test is a required 11.prev-c RED, and the opaque document-cursor encoding plus the numeric `Measurements` allowlist belong to 11.prev-d.

##### 11.prev-b1a — Schema v4 migration + backward compatibility + backup evidence

**Scope.** The additive local-only versioned migration to schema v4 (backup-before-migration through the established SQLite backup process); the `loader_command` and `loader_engine_instance` tables created **empty**, so that 11.prev-b1b can commit command receipts against an already-existing schema; the backward-compatibility/additive-only proof that every pre-existing table/index DDL and every pre-existing `run`/`run_document`/`audit_event` row is byte-identical after initialization; and engine-instance recording (create/refresh, never a counter reset). No command mutation, no receipt, and no precondition seam — those are 11.prev-b1b.

**Dependencies.** Consumes 11.prev-a (recorded limited acceptance (a)–(d)) and the Unit 10 store baseline; strictly after 11.prev-a. This slice refines the former 11.prev-b1 so the migration/backup surface lands sub-400; 11.prev-b1b depends on this slice.

**Allowed edit surface (strict).**

- `src/Rag.HistoricalLoader.Core/Persistence/SqliteStore.cs` — **required schema v4 surface**: `CurrentSchemaVersion` 3 → 4, the `SchemaV4` DDL (`loader_command`, `loader_engine_instance`, created empty), and the appended `new Migration(4, …)` entry. No other change in this file.
- new `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs` — the `SqliteControlStore` engine-instance surface (`RecordEngineInstanceAsync` plus its engine-instance type) only.
- new `tests/Rag.HistoricalLoader.UnitTests/Lifecycle/ControlStoreTests.cs` — migration/backup/additive-only/backward-compatibility/engine-instance groups only.
- `tests/Rag.HistoricalLoader.IntegrationTests/Sqlite/LifecyclePersistenceTests.cs` — **required schema v4 surface**: the single hard-coded `Assert.Equal(3, …)` becomes `4` and the test name `Initialize_MigratesSchemaV3WithBackupBeforeMigration` becomes `…V4…` (leaving the name unchanged would make it lie). No other change in this file.
- `apply-progress.md` (planning progress only).

**Must not touch:** `src/Rag.HistoricalLoader.Engine/**`, `src/Rag.HistoricalLoader.Contracts/**`, `NamedPipeHost.cs`, `Program.cs`, WPF/Desktop, CI workflows, `src/Rag.Companion/**`, `src/Rag.AdminApp*`, `src/Rag.Api/**`, Docker/Compose, `SqliteRunStore`, or any pre-existing dirty path; no new project, no `Rag.sln`/csproj change, no new package; no command-mutation/receipt surface (that is 11.prev-b1b).

**Line estimate.** ~190–260 (sub-400; refinement of the former 11.prev-b1).

- [x] RED (migration + backup evidence, integration): add failing tests asserting the additive v4 migration — a v3-seeded database migrates to `user_version == 4` with a `{db}.pre-migration-v3.bak` created through the established SQLite backup process (never a raw file copy while active); `loader_command` and `loader_engine_instance` exist and are empty; every pre-existing table/index DDL captured from `sqlite_master` before initialization is byte-identical after (additive-only proof); pre-existing `run`/`run_document`/`audit_event` rows are byte-identical; `PRAGMA foreign_keys` remains ON. <!-- sdd-owner: implementation -->
- [x] RED (backward compatibility): add failing tests asserting that pre-existing v3 read surfaces and data survive unchanged after `Initialize` — the pre-existing entities (`loader_installation`, `source_root`, `manifest`, `candidate`, `sample_set`, `benchmark_observation`, `run`, `run_document`, `audit_event`) are readable with identical values, a second `Initialize` on a migrated database is a no-op (`user_version` stays 4, no second `.bak`, v4 DDL byte-stable), and a migration whose backup step fails (fault-injected through the store's backup seam) leaves `user_version == 3` with no v4 tables — fail closed, never a silent rebuild. <!-- sdd-owner: implementation -->
- [x] RED (engine instance): add failing tests asserting `RecordEngineInstanceAsync("i1")` is reported by the store, then `"i2"` is reported with run/document/audit rows and counters unchanged (a refresh, not a counter reset). <!-- sdd-owner: implementation -->
- [x] GREEN: bump `SqliteStore.CurrentSchemaVersion` to 4, add `SchemaV4` (`loader_command`, `loader_engine_instance`, created empty), and append the `new Migration(4, …)` entry; update the schema-version assertion and its test name in `LifecyclePersistenceTests.cs`; create `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs` with the `SqliteControlStore` engine-instance surface (`RecordEngineInstanceAsync` and the engine-instance type); keep `SqliteRunStore` untouched; run `dotnet test tests/Rag.HistoricalLoader.UnitTests/Rag.HistoricalLoader.UnitTests.csproj` and `dotnet test tests/Rag.HistoricalLoader.IntegrationTests/Rag.HistoricalLoader.IntegrationTests.csproj` green. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with migration idempotency across a store close/reopen (no new `.bak`, `user_version` stays 4), an already-v4 database initializing without a further backup, the fault-injected backup-failure path staying at v3 with no v4 tables, byte-stable `loader_command`/`loader_engine_instance` DDL across reopen, and `RecordEngineInstanceAsync` after a reopen refreshing identity while leaving run/document/audit rows and counters untouched. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse the migration/schema and engine-instance types inside `Persistence/SqliteStore.cs` and `Lifecycle/ControlStore.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — 11.prev-b1a (limited acceptance: schema v4 migration + backward-compatibility/backup slice only):** (a) `ControlStoreTests.cs` and the updated `LifecyclePersistenceTests.cs` green; (b) the `SqliteStore.cs` change is limited to the v3→v4 constant, `SchemaV4`, and the migration entry, and the `LifecyclePersistenceTests.cs` change is the one version assertion plus its test name; (c) migration is additive with backup-before-migration evidence recorded and pre-existing DDL/rows byte-identical; (d) existing lifecycle/retry/audit tests remain green; (e) protected paths unchanged. **Decision gate — 11.prev-b1b does not start until (a)–(e) are recorded in `apply-progress.md`.** <!-- sdd-owner: implementation -->

##### 11.prev-b1b — Atomic command receipts: start/desired-state intents + audit mutation

**Scope.** The command commit path over the already-migrated v4 schema: start and desired-state (pause/resume intent) commands that commit the command mutation, the durable receipt, and the operator audit row **together** in one transaction through the single writer; store-owned replay/conflict/rejection semantics; the narrow typed precondition seam that keeps approval/target/capacity/one-active-run policy in the service; acknowledgement-boundary fault injection. The raw request is never persisted — only the normalized fingerprint. No schema, table, or backup work (all 11.prev-b1a).

**Dependencies.** Consumes 11.prev-b1a (recorded limited acceptance (a)–(e)); strictly sequential. No behaviour is removed from the former 11.prev-b1 by this split.

**Allowed edit surface (strict).**

- `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs` (extend with the command/receipt types, the named atomic composites `CommitStartAsync`/`CommitDesiredStateAsync`/`GetCommandReceiptAsync`, and the typed precondition delegates).
- `tests/Rag.HistoricalLoader.UnitTests/Lifecycle/ControlStoreTests.cs` (command atomicity/replay/conflict/fault-injection groups only).
- `apply-progress.md` (planning progress only).

**Must not touch:** `src/Rag.HistoricalLoader.Core/Persistence/SqliteStore.cs` (schema is already v4), `tests/Rag.HistoricalLoader.IntegrationTests/Sqlite/LifecyclePersistenceTests.cs`, `SqliteRunStore`, `src/Rag.HistoricalLoader.Engine/**`, `src/Rag.HistoricalLoader.Contracts/**`, `NamedPipeHost.cs`, `Program.cs`, WPF/Desktop, CI workflows, `src/Rag.Companion/**`, `src/Rag.AdminApp*`, `src/Rag.Api/**`, Docker/Compose, or any pre-existing dirty path; no new project, no `Rag.sln`/csproj change, no new package.

**Line estimate.** ~270–380 (sub-400; refinement of the former 11.prev-b1).

- [x] RED (command atomicity + replay): add failing tests asserting a start commit persists run + documents + exactly one operator audit row + receipt in one transaction; repeating the same `command_id` with the same normalized fingerprint returns `Replayed` with the durable receipt and zero additional run/document/audit rows; a changed fingerprint returns `Conflict` (`command_conflict`) with zero writes; a precondition rejection returns `Rejected` with a stable code and zero writes; the `loader_command` row stores only allowlisted fields (command id, normalized fingerprint, operation, run id, desired/observed state, created-at), and a sentinel raw request carrying an absolute path and a secret-shaped token never appears in any column. <!-- sdd-owner: implementation -->
- [x] RED (acknowledgement-boundary fault injection): add failing tests asserting `CommitStartAsync` with a plan containing a duplicate `(run_id, candidate_id)` raises a real `UNIQUE` violation mid-transaction and afterwards leaves zero run rows, zero documents, zero audit rows, and zero receipts (whole-transaction rollback); a precondition that throws leaves zero writes. The store deliberately does not pre-validate the plan; adding plan pre-validation removes the only honest acknowledgement-boundary test. <!-- sdd-owner: implementation -->
- [x] GREEN: extend `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs` with the named atomic composites (`CommitStartAsync`, `CommitDesiredStateAsync`, `GetCommandReceiptAsync`), the typed precondition delegates, and the durable command/receipt types (the engine-instance surface already exists from 11.prev-b1a); keep `SqliteRunStore` and `Persistence/SqliteStore.cs` untouched; run `dotnet test tests/Rag.HistoricalLoader.UnitTests/Rag.HistoricalLoader.UnitTests.csproj` and `dotnet test tests/Rag.HistoricalLoader.IntegrationTests/Rag.HistoricalLoader.IntegrationTests.csproj` green. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with the desired-state command path (pause/resume intents), a precondition re-check inside the transaction against a stale batch view (the bounded TOCTOU window), concurrent start commands through the single writer where exactly one wins and the loser writes nothing, and stable `command_conflict` rejection codes for unknown run id, invalid transition, and active-run contention. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse the command/receipt types and the store into `Lifecycle/ControlStore.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — 11.prev-b1b (limited acceptance: command receipt/intent slice only):** (a) the command groups in `ControlStoreTests.cs` are green and 11.prev-b1a's migration/backup/engine-instance groups remain green; (b) `Persistence/SqliteStore.cs` and `LifecyclePersistenceTests.cs` are unchanged in this slice; (c) the command commit is atomic (mutation + durable receipt + exactly one operator audit row, or nothing) with durable replay/conflict/rejection semantics; (d) existing lifecycle/retry/audit tests remain green; (e) protected paths unchanged. **Decision gate — 11.prev-b2 does not start until (a)–(e) are recorded in `apply-progress.md`.** <!-- sdd-owner: implementation -->

##### 11.prev-b2 — Coherent snapshot + inventory projection + privacy sentinel

**Scope.** One bounded coherent read transaction yielding the loader state snapshot (inventory totals/completeness, run desired/observed state, document counts by state, checkpoint time, derived safe block code, engine-instance ID, durable event high-water mark); the candidate-derived inventory projection that never presents an incomplete scan as complete; the privacy sentinel that keeps configuration snapshots and raw audit payloads out of serialized projections.

**Dependencies.** Consumes 11.prev-b1b (the v4 schema and engine-instance surface land in 11.prev-b1a); strictly sequential.

**Allowed edit surface (strict).** `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs` (extend with the snapshot/projection read surface); `tests/Rag.HistoricalLoader.UnitTests/Lifecycle/ControlStoreTests.cs` (snapshot/projection/privacy groups only); `apply-progress.md`. No `Persistence/SqliteStore.cs` change (schema is already v4); no other file.

**Line estimate.** ~230–330 (sub-400).

- [x] RED: add failing tests asserting the coherent snapshot comes from one bounded read — `HasRun`/`RunId` with desired/observed state; `DocumentCounts` by state where `sum == document count`; `CheckpointAt` from the run row; `BlockCode` derived from the durable observed state (`"blocked_auth"` iff observed state is `BlockedAuth`, otherwise null — no new column); engine-instance ID; durable event high-water mark; no run yields `HasRun == false` with empty counts while still reporting inventory; inventory completeness is `complete`/`incomplete`/`none` computed from `manifest.state` + `candidate` totals and **never** reports a running/incomplete scan as complete. <!-- sdd-owner: implementation -->
- [x] RED: add failing privacy-sentinel tests proving a seeded `configuration_snapshot` JSON (and any raw audit payload) never appears in the serialized snapshot, and that no absolute path, secret, or source key leaks into the projection. <!-- sdd-owner: implementation -->
- [x] GREEN: extend `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs` with `GetControlSnapshotAsync` and the candidate-derived inventory projection; derive `BlockCode` from the durable observed state; keep the read to one coherent transaction. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with interleaved document inserts plus 50 concurrent snapshot reads proving no mixed/incoherent counts, an installation with multiple manifests resolved to the latest (`start_time DESC, manifest_id DESC`), and an empty-store snapshot. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse the snapshot/projection types inside `Lifecycle/ControlStore.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Cross-slice obligation (recorded, not implemented here):** `ControlStoreVocabulary` duplicates `Contracts.ControlDocumentStates` / `ControlRunStates` / `ControlProtocol.Limits.MaxPageSize`, and no test in this slice can compare the two assemblies (Core must not reference Contracts; the integration project does not reference Contracts). The store↔wire vocabulary agreement test is a **required 11.prev-c RED**; without it the store and the wire can drift silently. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — 11.prev-b2 (limited acceptance: snapshot/projection slice only):** (a) snapshot/projection/privacy tests green; (b) inventory completeness never reports an incomplete scan as complete; (c) sentinel scan clean on the serialized snapshot; (d) existing lifecycle/retry/audit tests remain green; (e) protected paths unchanged. **Decision gate — 11.prev-b3 does not start until (a)–(e) are recorded in `apply-progress.md`.** <!-- sdd-owner: implementation -->
- [x] **Resolved decision — 11.prev-b2 `BlockCode` divergence (parent-owned; resolved as the total store projection):** the option chosen is the **total store projection**, not the narrow `BlockedAuth`-only derivation the earlier slice clause pinned. `ControlStoreVocabulary.BlockCode` now maps `RunObservedState.BlockedAuth → "blocked_auth"` and `RunObservedState.BlockedOperatorAction → "blocked_operator_action"`, and returns `null` for every other observed state; no new column is added and no state is invented. This supersedes the b2 clause's `"blocked_auth"` iff `BlockedAuth` formulation (left byte-intact in place as history), so `design.md`'s named `blocked_operator_action` code is now satisfied by the snapshot contract Unit 11 and 11.prev-c/-d consume. **Evidence (recorded by the parent):** authentic RED **1/6** → GREEN **6/6**; then independently, the focused block-code theory **6/6** (27 s), the focused `ControlStoreTests` run **66/66** (3 m 33 s), and the Release integration run **28/28** (32 s). The earlier combined independent verification timed out on aggregate duration, so these commands completed independently and are recorded separately; no figure is re-attributed to the timed-out run. The pre-existing `NU1903` warning remains. **Consequence:** this decision fixes the store block-code vocabulary that the required 11.prev-c store↔wire agreement test must pin; `#### 11.prev-c` is **not** started or modified by this record (its RED rows remain unchecked). <!-- sdd-owner: parent -->

##### 11.prev-b3 — Document keyset pages + event pages/cursors/resync

**Scope.** Individually coherent current-state document pages with a typed `DocumentPageCursor` and keyset ordering `(created_at, run_document_id)`, allowlisted fields only; bounded event pages ordered ascending with exclusive `after_event_id`, optional fixed `through_event_id`, page ≤100, next cursor, and high-water mark; total gap-safe cursor validity (retention floor and restore invalidation → `resync_required`, never a silent skip); raw `Measurements` numeric-or-null at the store boundary.

**Dependencies.** Consumes 11.prev-b2; strictly sequential.

**Allowed edit surface (strict).** `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs` (extend with the page/cursor surface); `tests/Rag.HistoricalLoader.UnitTests/Lifecycle/ControlStoreTests.cs` (document-page and event-page groups only); `apply-progress.md`. No `Persistence/SqliteStore.cs` change and no schema change; no production pruning code; no opaque cursor encoding and no measurements vocabulary (both 11.prev-d).

**Line estimate.** ~400–560 (at/over the 400-line review budget once tests travel with behaviour). **Delivery decision resolved: the user explicitly authorized an explicit `size:exception` for this slice in the current session with a 600-changed-line ceiling; the slice has not started, and no test, comment or evidence may be trimmed to fit (work-unit-commits rule).**

- [x] RED: add failing document-page tests asserting keyset paging by `(created_at, run_document_id)` with no duplicates and no gaps; current-state (not immutable-snapshot) pages, where a state change between pages is reflected by the later page; `Attempts` keys exactly `reserve/upload/commit/poll` with durable values; `limit <= 0` rejected; and no `source_document_key`, `extraction_hash`, remote ID, or configuration JSON in the serialized page. <!-- sdd-owner: implementation -->
- [x] RED: add failing event-page tests asserting ascending order; exclusive `after_event_id`; page capped at `ControlStoreVocabulary.MaxPageSize` (100); `NextCursor` only when the page filled the limit (null at the high-water mark); the optional fixed `through_event_id` bound honoured; run filtering; `HighWaterMark == MAX(event_id)`; every returned `Measurements` null-or-numeric; `InvalidRequest` for an out-of-range limit, `through < after`, or `through > high-water`; `ResyncRequired` for a cursor above the high-water mark (restore/rollback) and for a cursor below the derived retention floor; event IDs are durable monotonic installation-scoped cursors, not per-process sequence numbers. <!-- sdd-owner: implementation -->
- [x] GREEN: implement `GetDocumentPageAsync` (typed `DocumentPageCursor`, keyset SQL) and `GetEventPageAsync` (total gap-safe cursor validation; `MinRetainedEventId = COALESCE(MIN(event_id),0)`; `HighWaterMark = COALESCE(MAX(event_id),0)`) in `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs`; add no production pruning code and no schema change. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with retention-floor invalidation simulated by raw-SQL pruning (`DELETE FROM audit_event WHERE event_id <= k`) raising `MinRetainedEventId`; restore invalidation (newest events deleted, so a previously valid cursor sits above the high-water mark) → `ResyncRequired`; an exactly-at-limit page; an empty page at the high-water mark; and a non-numeric `Measurements` sentinel rejected at the boundary. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse the page/cursor types inside `Lifecycle/ControlStore.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — 11.prev-b3 (limited acceptance: page/cursor slice only):** (a) document-page and event-page tests green; (b) `resync_required` is total and gap-safe (retention and restore); (c) page serialization is allowlisted (no path/source key/config JSON); (d) existing lifecycle/retry/audit tests remain green; (e) protected paths unchanged. **Decision gate — 11.prev-b4 does not start until (a)–(e) are recorded in `apply-progress.md`.** <!-- sdd-owner: implementation -->
- [x] **Authorization record — 11.prev-b3 `size:exception` (planning only — the slice has NOT started):** the user explicitly authorized a `size:exception` for Unit 11.prev-b3 in the current session with a **600-changed-line ceiling**. Scope is this slice only (document keyset pages + event pages/cursors/resync + retention/restore invalidation) and is not renewed by any other slice; tests travel with the behaviour they verify and no test, comment or evidence may be trimmed to fit. <!-- sdd-owner: parent -->

##### 11.prev-b4 — Restart durability + regression safety net

**Scope.** Prove the whole store slice survives a real reopen: receipts, operator audit rows, loaded/terminal rows, per-operation attempt counters, and the three-attempt ceiling are preserved; snapshot counts are identical before and after; a cursor above the restored high-water mark returns `resync_required`; the full-solution regression net stays green.

**Dependencies.** Consumes 11.prev-b3; strictly sequential; closes the store slice.

**Allowed edit surface (strict).** new `tests/Rag.HistoricalLoader.IntegrationTests/Lifecycle/ControlStorePersistenceTests.cs`; `tests/Rag.HistoricalLoader.UnitTests/Lifecycle/ControlStoreTests.cs` (only if a unit-level durability assertion is needed); `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs` (minimal fix only, if a persistence defect is found — no new surface); `apply-progress.md`. No `Persistence/SqliteStore.cs` change; no other file.

**Line estimate.** ~150–250 (sub-400).

- [x] RED: add failing integration tests in `tests/Rag.HistoricalLoader.IntegrationTests/Lifecycle/ControlStorePersistenceTests.cs` asserting reopen durability — receipts, operator audit rows, loaded/terminal rows, per-operation attempt counters, and the three-attempt ceiling survive a store close/reopen; snapshot counts are identical before and after; a cursor above the restored high-water mark (simulated by deleting the newest events) returns `ResyncRequired`. <!-- sdd-owner: implementation -->
- [x] GREEN: no new production surface is expected; if a durability defect is exposed, apply the minimal fix inside `src/Rag.HistoricalLoader.Core/Lifecycle/ControlStore.cs` only. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: run the regression net — `dotnet build Rag.sln --configuration Release` (0 errors; pre-existing `NU1903` only), `dotnet test Rag.sln --configuration Release` green, existing loader unit/integration suites green, per-project test counts unchanged except the new cases; record the results. <!-- sdd-owner: implementation -->
- [x] REFACTOR: remove any test-only duplication introduced by the reopen fixtures; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — 11.prev-b4 (limited acceptance: restart durability + regression net only):** (a) `ControlStorePersistenceTests.cs` green; (b) receipts/audit/terminal rows/attempt counters/three-attempt ceiling preserved across reopen; (c) `dotnet build`/`dotnet test Rag.sln` Release green with only the pre-existing `NU1903`; (d) protected paths unchanged. <!-- sdd-owner: implementation -->

- [x] **Acceptance evidence — 11.prev-b (limited acceptance: store slice only, rolled up from b1a/b1b and b2–b4):** (a) focused store unit + SQLite integration tests green; (b) migration is additive and backup-before-migration evidence recorded; (c) existing lifecycle/retry/audit tests remain green; (d) protected paths unchanged. **Decision gate — 11.prev-c does not start until (a)–(d) are recorded.** <!-- sdd-owner: implementation -->

#### 11.prev-c — Control codec + dispatcher

**Consumes 11.prev-a (Contracts); sequenced strictly after 11.prev-b.** **Allowed edit surface (strict):** `src/Rag.HistoricalLoader.Engine/Control/**` (codec + dispatcher only; no host wiring, no pipe opening); additive `ProjectReference` from Engine to Contracts; new `tests/Rag.HistoricalLoader.UnitTests/Control/FrameCodecTests.cs` and `DispatchValidationTests.cs`; `apply-progress.md`. No `Program.cs`/`NamedPipeHost.cs` changes (those are 11.prev-d/e); no Core/WPF/CI/protected-path changes.

- [x] RED: add failing tests asserting byte-mode framing and dispatch validation: four-byte unsigned little-endian byte length + UTF-8 JSON round trips; partial/truncated/oversized frames (1 MiB limit) rejected **before allocation**; malformed JSON and JSON depth >16 rejected; requested page >100 rejected; unknown version/operation and malformed required fields fail closed before dispatch; optional additive fields ignorable; cancellable bounded read/write deadlines; timeout/saturation rejection never dispatches; the dispatcher validates and delegates only (no ingestion policy, no path-bearing inputs, no caller-selected pipe/database path, no newline framing, no binary object serialization, no arbitrary type names, no raw exception payloads). <!-- sdd-owner: implementation -->
- [x] RED (recorded by 11.prev-b2): add the store↔wire vocabulary agreement test comparing `ControlStoreVocabulary` with `Contracts.ControlDocumentStates` / `ControlRunStates` / `ControlProtocol.Limits.MaxPageSize` (test-side comparison; Core must not reference Contracts), proving the store and the wire cannot drift silently. <!-- sdd-owner: implementation -->
- [x] GREEN: implement the codec + dispatcher under `src/Rag.HistoricalLoader.Engine/Control/` against an abstract stream/transport seam so Linux tests run with fake streams; additively reference Contracts from the Engine project; record explicit default deadline/connection/request limits. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: extend tests with empty frame, exactly-at-limit frame, multi-byte UTF-8 truncation, valid depth-16 vs. rejected depth-17 payload, instrumented-stream proof that rejection never allocates beyond the limit, stable `error_code` rejection instead of raw exception text, and bounded connection/request-count saturation at the dispatcher seam. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse codec/dispatcher types under `Control/Protocol.cs` and `Control/Dispatcher.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — 11.prev-c (limited acceptance: codec/dispatcher slice only):** (a) framing/validation/saturation tests green on Linux with fake streams; (b) existing CLI behavior unchanged; (c) no pipe/ACL/Windows code and no host wiring introduced; (d) `dotnet test Rag.sln` green. **Decision gate — 11.prev-d does not start until (a)–(d) are recorded.** <!-- sdd-owner: implementation -->

#### 11.prev-d — Control service + supervised `serve` host

**Consumes 11.prev-b (receipts/projections) and 11.prev-c (codec/dispatcher).** **Allowed edit surface (strict):** `src/Rag.HistoricalLoader.Engine/Control/**` (service, safe projections, host supervision, installation lock; fake transport in this slice); additive `serve` subcommand in `src/Rag.HistoricalLoader.Engine/Program.cs` (existing `inventory`/`select-sample`/`benchmark extraction` commands preserved); new `tests/Rag.HistoricalLoader.UnitTests/Control/ControlServiceTests.cs`, `HostSupervisionTests.cs`, `RecoveryTests.cs`, `PrivacySentinelTests.cs`; `tests/Rag.HistoricalLoader.IntegrationTests/Control/ControlServiceRecoveryTests.cs`; `apply-progress.md`. No Windows pipe implementation (11.prev-e); no Core/WPF/CI/protected-path changes.

- [x] RED: add failing service tests (fake transport + fake extractor/API against the real host service) asserting: `hello` must succeed before other operations; `start` resolves the approved batch and secret-free configuration inside the engine, validates approval/target/capacity, and atomically creates run/documents, start audit, and receipt; one active run per installation, and an already-existing run with a conflicting batch is rejected; `pause` commits pause intent and operator audit before `accepted` and does not wait for drain on the request thread (acknowledgement is not a confirmed checkpoint); `pausing` is exposed while draining and `paused` only after the durable safe-boundary check; `resume` resumes only eligible paused/recovered work (attempts and terminal outcomes preserved) and blocked auth/operator conditions cannot be bypassed by a wire flag; `get_state` projects durable rows only (counts, checkpoint time, safe block code, engine-instance ID, event high-water mark), with no run as an explicit empty result; `get_documents` pages are bounded and allowlisted (candidate/run-document IDs, state, per-operation attempt counts, safe classification/timestamps only — never source key/path, extracted text, configuration JSON, or secret material); `get_events` is the bounded pull-based v1 event feed with exclusive `after_event_id`, page limit, optional fixed `through_event_id`, next cursor, and high-water mark, with event fields limited to the allowlist (durable event ID, UTC time, run/candidate IDs, allowlisted action/state transition/outcome/classification, attempt number, allowlisted numeric measurements) and persisted `ConfigurationSnapshot`/raw audit payloads never forwarded; command serialization covers only short validation/transactions, never the awaited entire pipeline run, so pause remains serviceable; no double pipeline after concurrent commands, disconnect, repeated command, or lost response (replaying the same command ID returns the durable receipt and never restarts a recovered run); a second engine instance fails safely on the OS-backed exclusive installation lock before opening the store or pipe; a disconnect cancels only IPC work; storage failure yields a safe unavailable/error response and blocks mutation acknowledgement (an undurable success is never sent); host shutdown requests a safe pause and drains within a configured timeout, and timeout/kill leaves recovery work rather than fabricated `paused`/`loaded`. <!-- sdd-owner: implementation -->
- [x] GREEN: implement the control service, safe projections, host supervision, and installation lock under `src/Rag.HistoricalLoader.Engine/Control/`, and wire the explicit `serve` composition entry point in `Program.cs`; compose the existing `HistoricalPipeline.RunAsync`/`RequestPauseAsync`/`ResumeAsync` (never duplicate lifecycle or retry logic); one supervised pipeline task runs at a time; the host supervises scheduling independently of the client connection; record the engine-instance ID (new each process); use the fake transport in this slice. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: fault-injection and projection tests — crash around acknowledgement boundaries preserves atomic intent/audit/receipt; restart plus explicit resume preserves loaded/terminal rows and the three-attempt ceiling; a pipeline method returning does not produce `loaded` for outstanding `remote_pending` work (counts derive from durable rows, never scheduler-local counters); new engine-instance ID triggers a state refresh, not a counter reset; invalid cursor → `resync_required`; slow reader bounded; unavailable store surfaced safely; persistent process faults surface safe error codes without raw exception text; privacy sentinel scans over every serialized success/failure/snapshot/event payload prove content, absolute paths, source keys, credential names/values, tokens, free-form diagnostics, and raw exception text are absent. <!-- sdd-owner: implementation -->
- [x] REFACTOR: collapse service/projection types into `Control/Service.cs` and `Control/Projections.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [x] **Acceptance evidence — 11.prev-d (limited acceptance: service/host slice only):** (a) service/runtime-recovery/privacy suites green against fakes on Linux; (b) existing CLI commands unchanged and green; (c) no real pipe opened in this slice (fake transport); (d) protected paths unchanged. **Decision gate — 11.prev-e does not start until (a)–(d) are recorded.** <!-- sdd-owner: implementation --> <!-- evidence exception (d): the protected-path clause is closed by the recorded evidence exception, NOT verified path-level; see apply-progress.md "Work unit (current) — 11.prev-d closure under a user-authorized protected-path evidence exception" -->
- [x] **Recorded cross-slice obligations (from the 11.prev-b store slice):** (i) the opaque document-cursor encoding plus the numeric `Measurements` allowlist are service-layer concerns implemented and tested in this slice; (ii) decide and test the wire presentation of an unrecorded engine instance (the store snapshot reports `EngineInstanceId == null` rather than inventing an empty identity). <!-- sdd-owner: implementation -->

#### 11.prev-e — Windows named-pipe transport + pipe security evidence

**Consumes 11.prev-d.** **Allowed edit surface (strict):** `src/Rag.HistoricalLoader.Engine/Control/**` Windows transport factory (+ ACL/peer-identity helpers) behind an OS guard; replacement of the non-listening stub in `src/Rag.HistoricalLoader.Engine/NamedPipeHost.cs`; new `tests/Rag.HistoricalLoader.UnitTests/Control/NamedPipeTransportTests.cs` (Linux-testable pieces via the transport seam); new evidence file `docs/historical-ingestion-rebaseline/unit-11-prev-windows-pipe-security.md`; `apply-progress.md`. No Core/WPF/CI/protected-path changes; no deployment; no real corpus run.

- [x] RED: add failing tests (via the transport seam) asserting: bounded read/write deadlines are cancellable; bounded connection/request counts and saturation are rejected without dispatch; a pipe-name collision fails closed; the production pipe path fails closed with no localhost TCP, public pipe, or anonymous fallback; a caller-selected pipe/database path is rejected; on Linux the production invocation returns `platform_not_supported` **before** touching Windows credentials or starting ingestion. <!-- sdd-owner: implementation --> <!-- evidence (Linux CI): the RED contract is tests/Rag.HistoricalLoader.UnitTests/Control/NamedPipeTransportTests.cs, and the ci-pr Validate job ran the full solution test suite green on ubuntu-latest (PR #33, run 35744364129), which is where these Linux assertions actually execute. -->
- [x] GREEN: implement the Windows named-pipe transport factory (byte-mode asynchronous pipe read/write; local-machine/current-user owner-restricted ACL; explicit remote-client rejection; peer identity verification on both ends; endpoint from a fixed prefix + opaque installation/user identity) and replace `NamedPipeHost.cs`; the production pipe opens only under explicit `serve`. <!-- sdd-owner: implementation --> <!-- evidence: the transport factory, the owner-restricted protected ACL, the explicit remote-client rejection, the peer-SID check and the derived endpoint are all implemented, and run 2 of the Windows gate proved them on a real host (E1, E2, E5, E6 and E7 pass). -->
- [x] TRIANGULATE: extend tests with slow-reader/saturation sequences, foreign-user endpoint substitution rejection (where enforceable), and fail-closed behavior when ACL/identity enforcement is unavailable. <!-- sdd-owner: implementation --> <!-- evidence: the saturation, idle-peer and unavailable-identity fail-closed sequences are covered and green in the Linux CI job (PR #33, run 35744364129). The foreign-user substitution clause rests on the recorded E3 evidence exception above, NOT on an executed foreign-user attempt. -->
- [x] **Windows-only acceptance evidence (operator Windows machine required — recorded, not assumed):** `docs/historical-ingestion-rebaseline/unit-11-prev-windows-pipe-security.md` records: same-user connection succeeds; foreign-user connection denied; remote/non-local connection denied; ACL enforcement verified; fail-closed with no TCP/public/anonymous fallback; production round trip smoke under `serve`. The record states explicitly that Linux portable/fake-transport tests **do not** prove Windows ACL enforcement. <!-- sdd-owner: implementation --> <!-- evidence: the gate RAN on a real Windows host (2026-09-22, `<hostname>`). **Run 1** at commit `1b89a6ea538dd590935605327b022914f04e8a5d`: E2, E5 and E6 passed; **E1 and E7 FAILED** on blocking finding BF-1 (`WindowsNamedPipeTransport.TryVerifyPeer` impersonated before any byte was read, so `RunAsClient` threw `ERROR_CANNOT_IMPERSONATE`/1368 and every peer — including the operator — was denied); E3 and E4 were NOT EXECUTED. **Run 2** on the BF-1 fix: **E1, E2, E5, E6 and E7 pass**, E3 and E4 still not executed. The fix adds `Control/PeerPrefixStream.cs`, reorders the accept path in `Control/WindowsPipeTransport.cs` to read one byte, verify, then replay it, and adds six Linux cases. Full evidence, both runs, and findings F-1 (the protocol's per-connection `hello` handshake gate makes the run guide's one-connection-per-call client unable to reach `get_state`, so E7 must use a same-connection client) and F-2 (3 of 425 Linux-suite cases cannot pass on Windows) are in `docs/historical-ingestion-rebaseline/unit-11-prev-windows-pipe-security.md` §4/§5/§5.4/§7 and `apply-progress.md` "Work unit (current) — 11.prev-e BF-1 fix and Windows gate re-run". **The box IS ticked, and only because the maintainer authorized the recorded evidence exception: acceptance letter (b) requires foreign-user denial and remote/non-local denial, and E3/E4 were never executed, so those two letters are carried by the exception and NOT verified. A later reader must not read this tick as evidence of those two facts; retire the exception by executing E3 and E4. --> <!-- evidence exception (11.prev-e E3/E4): foreign-user denial and remote/non-local denial are closed by the recorded user-authorized evidence exception, NOT verified - no foreign-user or remote connect attempt has ever been executed against this boundary on any host. The residual risk is stated in docs/historical-ingestion-rebaseline/unit-11-prev-windows-pipe-security.md section 6 (L8) and in apply-progress.md "Work unit (current) - 11.prev-e E3/E4 closure under a user-authorized evidence exception". -->
- [x] REFACTOR: collapse transport types under `Control/WindowsPipeTransport.cs`; re-run `dotnet test Rag.sln --no-build` and confirm green on Linux. <!-- sdd-owner: implementation --> <!-- evidence: all five transport types are collapsed under Control/WindowsPipeTransport.cs, and the ci-pr Validate job ran the full solution tests green on ubuntu-latest (PR #33, run 35744364129), which is the Linux confirmation this row asks for. Deliberate deviation: the new portable PeerPrefixStream lives in its own file Control/PeerPrefixStream.cs because it is platform-neutral and must stay reachable from the test project. -->
- [x] **Acceptance evidence — 11.prev-e (limited acceptance: transport slice only):** (a) Linux seam tests green; (b) Windows pipe security evidence recorded in the named file; (c) default Linux solution build/test unaffected (no `EnableWindowsTargeting`/`UseWPF`/conditional test omission); (d) existing CLI unchanged. **Decision gate — proceeds to the full Unit 11.prev prerequisite gate below.** <!-- sdd-owner: implementation --> <!-- evidence: letter (b) is **still NOT satisfied**, but the reason changed. BF-1 is FIXED and re-verified: E1, E2, E5, E6 and E7 pass on the fixed build. What remains is purely environmental — **E3 (foreign-user denial) and E4 (remote/non-local denial) were never executed** because the host has no second Windows account whose credentials the operator holds and no second machine on the network. See `docs/historical-ingestion-rebaseline/unit-11-prev-windows-pipe-security.md` §5/§5.4/§7. Letters (a), (c) and (d) stand on the previously recorded Linux runs. **No checkbox was marked and the decision gate is not crossed**: do not start Unit 11 until E3 and E4 are executed on a real host. --> <!-- evidence exception (11.prev-e E3/E4): foreign-user denial and remote/non-local denial are closed by the recorded user-authorized evidence exception, NOT verified - no foreign-user or remote connect attempt has ever been executed against this boundary on any host. The residual risk is stated in docs/historical-ingestion-rebaseline/unit-11-prev-windows-pipe-security.md section 6 (L8) and in apply-progress.md "Work unit (current) - 11.prev-e E3/E4 closure under a user-authorized evidence exception". -->

**Full prerequisite gate — exact Unit 11 blocking gate**

- [x] **Acceptance evidence — Unit 11.prev (full prerequisite gate):** (a) Linux `dotnet restore/build/test Rag.sln` green with only the additive portable Contracts registration and no Windows desktop targeting/conditional test omission; (b) version/framing round trips and partial/truncated/oversized/malformed/deep-JSON/unknown-operation/version rejection without dispatch; (c) start/pause/resume through the dispatcher to the real host service with fake extractor/API — no double pipeline after concurrent commands, disconnect, repeated command, or lost response; conflict fingerprint and missing/invalid run rejected; (d) transaction failure/crash around acknowledgement boundaries preserves atomic intent/audit/receipt; restart and explicit resume preserve loaded/terminal rows and the three-attempt ceiling; (e) pause stays responsive while a pipeline is active; accepted/pausing/paused differ; remote pending never becomes loaded by projection; blocked conditions cannot be bypassed by resume; (f) coherent counts/event high-water, bounded pages, replay/deduplication, reconnect/new engine instance, invalid-cursor `resync_required`, slow reader, and unavailable-store behavior; (g) privacy sentinel scans clean on every serialized success/failure/snapshot/event payload; (h) Windows named-pipe round trip and ACL/peer/remote denial evidence recorded (Linux portable tests cannot substitute). **Exact blocking gate — Unit 11 (WPF shell) MUST NOT start until (a)–(h) are all recorded in `apply-progress.md`. Partial slice acceptance does not unblock Unit 11. This gate does not replace Windows operator or Gate L evidence.** <!-- sdd-owner: implementation --> <!-- evidence exception (11.prev-e E3/E4): foreign-user denial and remote/non-local denial are closed by the recorded user-authorized evidence exception, NOT verified - no foreign-user or remote connect attempt has ever been executed against this boundary on any host. The residual risk is stated in docs/historical-ingestion-rebaseline/unit-11-prev-windows-pipe-security.md section 6 (L8) and in apply-progress.md "Work unit (current) - 11.prev-e E3/E4 closure under a user-authorized evidence exception". -->
- [ ] **Delivery-decision record — Unit 11.prev (partially resolved; parent-gated):** the design amendment authorized no exception by itself; the current-session `ask-on-risk` pause has since been taken along the chain and the maintainer recorded slice-level decisions: (i) `size:exception` for 11.prev-a (~560 measured); (ii) `size:exception` for 11.prev-b1a (measured overrun of its 300-line attempt bound); (iii) `size:exception` for 11.prev-b1b (attempt overran its budget; completed through the narrower evidence attempt); (iv) measured `size:exception` for 11.prev-b2 (~594); (v) explicit `size:exception` for 11.prev-b3 with a **600-changed-line ceiling**; (vi) **the user explicitly authorized an explicit `size:exception` for 11.prev-d (~500–750) in the current session (planning only — the slice has NOT started)**; (vii) **the user explicitly authorized an explicit `size:exception` for 11.prev-e in the current session (planning only — the slice has NOT started)**, forecast above the 400-line review budget because the Windows named-pipe transport, its pipe-security evidence and the tests that verify that behaviour travel together in one indivisible work unit. Each of the (vi) and (vii) decisions is limited to **its own slice only** — 11.prev-d: control service + supervised `serve` host, safe projections, installation lock, recovery, privacy sentinel; 11.prev-e: Windows named-pipe transport factory + ACL/peer-identity/remote-denial pipe security evidence + Linux seam tests — and is **not renewed** by any other slice; tests travel with the behaviour they verify and no test, comment or evidence may be trimmed to fit, and re-slicing is not implied by either record. This record **does not authorize delivery** (no commit, push, PR, or release is approved) and **does not authorize Unit 11**: the 11.prev-e decision changes only its review-budget status and leaves the Unit 11.prev full prerequisite gate (a)–(h) binding; 11.prev-d's apply remains additionally gated by 11.prev-c's recorded limited acceptance, and 11.prev-e's apply by 11.prev-d's. Each slice remains blocked by its predecessor's recorded limited acceptance. <!-- sdd-owner: parent -->

### Unit 11 — Replaceable WPF operator shell

**Blocked by the Unit 11.prev full prerequisite gate above.** Provisional Windows shell consuming the verified Unit 11.prev control contract (`ILoaderControlClient` + version-1 wire contract): the shell references the portable Contracts library and must not duplicate Desktop-local wire models, reference Engine/Core, or invent an incompatible engine dependency. WPF stays behind the engine contract; UI technology may be revisited after Gate L with evidence (accessibility, packaging, operator feedback) — never as a Gate L blocker. Likely `size:exception` (not renewed by the 11.prev amendment).

- [ ] RED: add failing view-model tests in `tests/Rag.HistoricalLoader.UnitTests/Desktop/InventoryViewModelTests.cs`, `RunViewModelTests.cs`, `AuditViewModelTests.cs`, `ErrorViewModelTests.cs` asserting each view-model binds to engine events without opening source folders, SQLite, reusable secrets, or the remote API; that the inventory view renders persisted manifest totals; that the run view distinguishes `pending`, `pausing`, `paused`, `running`, `remote_pending`, `loaded`, `skipped_document_error`, `retry_exhausted_network`, `blocked_auth`, `blocked_operator_action`; that the audit view shows allowlisted fields only; that the error view shows error codes and classification without content/paths/secrets/tokens. <!-- sdd-owner: implementation -->
- [ ] RED: add failing UI automation tests proving the shell opens inventory, starts a run, pauses, resumes, and inspects states without terminal use. <!-- sdd-owner: implementation -->
- [ ] GREEN: create `src/Rag.HistoricalLoader.Desktop/Rag.HistoricalLoader.Desktop.csproj` (WPF, .NET 10, win-x64), `App.xaml`, `MainWindow.xaml`, view-models under `ViewModels/`, and a named-pipe client that consumes the engine's versioned local contract. Register in `Rag.sln`. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: extend tests with view-model state propagation during a paused→kill→restart→resume sequence, with audit allowlist enforcement, and with the operator action log being recorded as `audit_event` rows. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: collapse view-models into a single `ViewModels/` namespace; re-run `dotnet test Rag.sln --no-build` and confirm green. <!-- sdd-owner: implementation -->
- [ ] **Acceptance evidence — Unit 11:** (a) UI automation/view-model tests are green; (b) the shell never opens source folders/SQLite/secrets/API directly; (c) `apply-progress.md` records the operator-tested inventory/start/pause/resume/state/audit/error flow; (d) WPF remains the provisional choice and Electron is not selected; (e) the Unit 11.prev full prerequisite gate (a)–(h) was recorded before this unit started, and Windows UI-automation evidence remains separate from Linux portable tests. **Decision gate — Unit 12 does not start until the local vertical proof with at least 100 documents is recorded.** <!-- sdd-owner: implementation -->

### Unit 12 — Windows local vertical proof + Gate L evidence pack

- [ ] RED: add a Gate L end-to-end test in `tests/Rag.HistoricalLoader.IntegrationTests/GateL/LocalProofTests.cs` driving at least 100 selected proof documents through inventory → benchmark-aligned extraction → reservation/stream/commit → polling → legacy search with attributable provenance; pause reaches a durable checkpoint; kill/restart resumes without reloading confirmed documents; transient fault injection proves no fourth attempt; document faults do not block later documents; idempotent unknown-outcome reconciliation and hash-conflict tests pass; token expiry, insufficient scope, application revocation, and Cloudflare-local-exception labeling are verified; privacy sentinel scan passes; only loopback API access is used locally. <!-- sdd-owner: implementation -->
- [ ] GREEN: assemble `docs/historical-ingestion-rebaseline/gate-l-evidence.md` from `apply-progress.md` records — inventory accounting, benchmark report (Unit 3), Companion disposition (Unit 5), ≥100 documents into legacy with search/provenance, pause/kill/restart safety, retry/document fault tests, idempotent ambiguity recovery, auth scope/expiry/revocation evidence, privacy scan, loopback-only proof, real-time compatibility (existing tests green). <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: cross-check every Gate L requirement against the recorded evidence; record any missing item as an explicit blocker. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: consolidate the Gate L evidence doc with the design's Gate L requirements table. <!-- sdd-owner: implementation -->
- [ ] **Acceptance evidence — Unit 12 / Gate L:** (a) Gate L evidence pack is complete; (b) `apply-progress.md` records the at-least-100-document proof, the search/provenance evidence, the pause/kill/restart evidence, and the privacy scan; (c) real-time compatibility tests remain green; (d) the legacy collection contains the proven documents with provenance; (e) no full-corpus run, no production deployment, no AdminApp integration. **Decision gate — Unit 13 (Gate A) does not start until every Gate L requirement is satisfied.** A Gate L failure blocks OCI/Dokploy validation. <!-- sdd-owner: implementation -->

## Tranche 3 — Environment validation (Units 13–14)

Environment-validation units produce **evidence**, not authorization. They do not start the full corpus; they do not deploy to production; they do not enable RDD.

### Unit 13 — OCI/Dokploy ARM64 bounded validation + Gate A

- [ ] RED: add failing environment-contract tests in `tests/Rag.HistoricalLoader.IntegrationTests/GateA/Arm64ContractTests.cs` proving the same contract/fault suite produces evidence for the OCI/Dokploy ARM64 environment as for local, with native `linux/arm64` API/operator/runtime images and exact immutable image identity recorded. <!-- sdd-owner: implementation -->
- [ ] GREEN: assemble `docs/historical-ingestion-rebaseline/gate-a-evidence.md` — verified native ARM64 images, migration/rollback/restore rehearsal, readiness, internal-only PostgreSQL/llama access, Cloudflare service-token allow/deny and direct-origin denial, historical scope/revocation/idempotency/provenance contract tests, llama.cpp CPU embedding observations on the actual ARM64 host (resource headroom, real-time queue impact), comparison with local evidence and documented regressions/limitations, no dependency on a Windows Companion binary on the server. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: cross-check every Gate A requirement; record any missing item as a blocker. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: consolidate the Gate A evidence doc with the design's Gate A requirements table. <!-- sdd-owner: implementation -->
- [ ] **Acceptance evidence — Unit 13 / Gate A:** (a) Gate A evidence pack is complete; (b) `apply-progress.md` records ARM64 image identity, Cloudflare allow/deny, direct-origin denial, llama.cpp CPU observations, and real-time impact; (c) infrastructure changes (if any) are isolated from AdminApp WIP and separately reviewed. **Decision gate — Unit 14 (Gate C) does not start until every Gate A requirement is satisfied.** A Gate A failure blocks Coolify validation. <!-- sdd-owner: implementation -->

### Unit 14 — Coolify target validation + Gate C + full-corpus decision packet

- [ ] RED: add failing environment-contract tests in `tests/Rag.HistoricalLoader.IntegrationTests/GateC/CoolifyContractTests.cs` proving the same contract/fault suite produces evidence for the Coolify target environment with signed/verified immutable multi-platform image references and target architecture. <!-- sdd-owner: implementation -->
- [ ] GREEN: assemble `docs/historical-ingestion-rebaseline/gate-c-evidence.md` — signed/verified immutable multi-platform image references, secret/config inventory (no secret in Compose, manifests, logs, or reports), Cloudflare-only ingress and no direct internal-service exposure, additive migration and backup/restore evidence, bounded proof-run checkpoint/auth/provenance/search evidence matching prior gates, observability and capacity comparison with staging, explicit rollback procedure, and confirmation that no full-corpus run occurred. <!-- sdd-owner: implementation -->
- [ ] GREEN: assemble `docs/historical-ingestion-rebaseline/full-corpus-decision-packet.md` — the **decision document** that may authorize or reject scaling, citing inventory, weighted capacity range, operator window, reliability evidence, infrastructure headroom, Companion disposition, rollback/backup readiness, and unresolved risks. **This task does not authorize scaling; it produces the packet for a separate future decision.** <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: cross-check every Gate C requirement; record any missing item as a blocker. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: consolidate the Gate C evidence doc and decision packet with the design's Gate C and Open-evidence-decisions tables. <!-- sdd-owner: implementation -->
- [ ] **Acceptance evidence — Unit 14 / Gate C:** (a) Gate C evidence pack is complete; (b) full-corpus decision packet is documented and **records the explicit decision that no full-corpus run occurs in this change**; (c) `apply-progress.md` records image identity, secret hygiene, ingress evidence, migration/backup, and rollback; (d) no production deployment, no full-corpus run, no AdminApp integration in this change. **Decision gate — Full-corpus and production deployment require a separate decision record, citing the Gate L/A/C evidence, and are not authorized by this change.** <!-- sdd-owner: implementation -->

## Tranche 4 — Future separate change (NOT in this change)

### Unit 15 — AdminApp provisioning/revocation integration (future, separate change)

Out of scope for this change. May begin only after direct-auth API proof (Unit 8) and Gate L (Unit 12) pass. Future work must: (a) integrate AdminApp as a **control plane** for provisioning and revoking loader client credentials, never as a host, UI shell, secret store, filesystem agent, or fallback orchestrator; (b) preserve AdminApp WIP intact; (c) own its own source, Compose, CI, security, and review gates; (d) never traverse or ingest local files; (e) cannot retroactively block Gate L. **No task in this change implements Unit 15.** <!-- sdd-owner: parent -->

## Out-of-scope reminder (every unit)

Every unit above is bound by these non-negotiables. Tasks do not authorize them; the parent validates them at the corresponding gate.

- No full-corpus run, no production deployment, no automatic taxonomy, no GPU or external embedding service, no Broker/distributed extractor, no new internal-service endpoint, no AdminApp integration, no modification of AdminApp WIP, no modification of `src/Rag.Companion/` until Companion disposition authorizes it.
- No RDD enablement, no tag, no push, no commit on behalf of the user, no manual publication, no CI change beyond additive tests invoked by `dotnet test Rag.sln`.
- The WPF decision is provisional; Electron is not selected and is not a precondition.
- The historical normalized-text size cap is an explicit configuration chosen from evidence, not inherited from the 1 MiB real-time cap, not unlimited.
- Worker concurrency on the server is preserved at current values until llama.cpp/DB/real-time impact evidence permits change.

## Parent-only actions

- [x] Chain strategy confirmed: `feature-branch-chain`. Recorded `size:exception` decisions for already-applied units keep only their recorded scope and are **not renewed** by the Unit 11.prev amendment. The required current-session `ask-on-risk` pause for Unit 11.prev has been taken along the chain; the maintainer recorded slice-level decisions (11.prev-a and 11.prev-b2 measured exceptions; 11.prev-b1a and 11.prev-b1b overrun decisions with b1b completed via the narrower evidence attempt; 11.prev-b3 explicit 600-changed-line-ceiling exception). 11.prev-d still requires its delivery decision (explicit `size:exception` or approved re-slice) **before its apply**, and 11.prev-c/-e stay threshold-flagged. <!-- sdd-owner: parent -->
- [x] Chain strategy cached in the active artifact store. The **first authorized implementation unit is Unit 1a** (central package + minimal Core + SQLite baseline); Units 1b and 1c follow sequentially. The prior blocked attempt is preserved as historical evidence in `apply-progress.md` and is **not modified** by this revision. <!-- sdd-owner: parent -->
- [ ] Validate pre-commit receipt before any commit is staged for this change. Existing real-time route, AdminApp WIP, AdminApp Docker/Compose/CI, and `src/Rag.Companion/` must remain unchanged in the staged content unless a Companion-disposition record (Unit 5) explicitly authorizes a bounded follow-up. Unit 1a may additively modify `Directory.Packages.props` (one new `PackageVersion` for `Microsoft.Data.Sqlite`) and `Rag.sln` (additive Core + unit-test project registration only); nothing else. <!-- sdd-owner: parent -->
- [ ] Validate pre-push receipt before any push attempt (no tag, no manual publication, no RDD enablement). <!-- sdd-owner: parent -->
- [ ] Validate pre-PR receipt before opening the chosen chain slice (each slice targets its predecessor's branch in `feature-branch-chain`, or merges to `main` in `stacked-to-main`; only the final slice merges to `main` in the chain). The first slice is Unit 1a. <!-- sdd-owner: parent -->
- [ ] Start or reuse bounded review once each chain slice is fully implemented and locally verified (post-apply). <!-- sdd-owner: parent -->
- [ ] Enforce evidence gates: Unit 1b blocked until Unit 1a green; Unit 1c blocked until Unit 1a **and** Unit 1b green; Unit 2 blocked until Unit 1c green; Unit 3 blocked until Unit 2 + sample parameters recorded; Unit 5 blocked until Units 3 + 4 evidence recorded; Unit 6 blocked until Unit 5 disposition recorded; Unit 7 blocked until Unit 6 green; Unit 8 blocked until Unit 7 + real-time tests green; Unit 9 blocked until Unit 8 streaming/idempotency green; Unit 10 blocked until Unit 9 privacy/auth-block green; **Unit 11.prev blocked until Unit 10 bounded pipeline green; Unit 11.prev-a → b1a → b1b → b2 → b3 → b4 → c → d → e strictly sequential, each blocked by the previous slice's limited acceptance recorded in `apply-progress.md`; Unit 11 blocked until the Unit 11.prev full prerequisite gate (a)–(h) is recorded, including the Windows named-pipe security evidence**; Unit 12 (Gate L) blocked until Unit 11 view-model/UI green; Unit 13 (Gate A) blocked until Gate L evidence complete; Unit 14 (Gate C) blocked until Gate A evidence complete; full-corpus run is **never** authorized by this change. <!-- sdd-owner: parent -->
- [ ] Archive the change only after Gate C evidence is complete and the full-corpus decision packet explicitly records that no full-corpus run occurred in this change. <!-- sdd-owner: parent -->

## Dependencies

- **Inventory pilot (1a → 1b → 1c) is strictly sequential:** Unit 1b depends on Unit 1a being green with its acceptance evidence recorded in `apply-progress.md`; Unit 1c depends on both Unit 1a and Unit 1b being green. Within Unit 1a, the `Directory.Packages.props` `Microsoft.Data.Sqlite` declaration is a precondition for any Core/UnitTests work; the Core project registration in `Rag.sln` is a precondition for the SQLite bootstrap; the SQLite bootstrap + entities are a precondition for the persistence/classification tests. Unit 1a's edit surface is strict and must not touch Engine, integration tests, or any WIP-protected path.
- **Tranche 1 (Units 1a/1b/1c and 2–5) is sequential:** each unit depends on the previous unit being green with acceptance evidence recorded in `apply-progress.md`. Units 2–5 do not start before Unit 1c is green.
- **Tranche 2 (Units 6–12, including 11.prev) is sequential;** Unit 6 depends on Unit 5's `IExtractor` interface; Unit 7 depends on Unit 6's state machine; Unit 8 depends on Unit 7's token-exchange extension; Unit 9 depends on Unit 8's API contract; Unit 10 depends on Unit 9's API client + WinCred + Cloudflare headers; **Unit 11.prev depends on Unit 10's bounded pipeline** and consumes existing prepared batches (it composes `HistoricalPipeline.RunAsync`/`RequestPauseAsync`/`ResumeAsync` and never duplicates lifecycle/retry logic); **Unit 11.prev slices are strictly sequential (11.prev-a → b1a → b1b → b2 → b3 → b4 → c → d → e), each blocked by the previous slice's limited acceptance**; **Unit 11 depends on Unit 11.prev's verified control contract — no longer directly on Unit 10**; Unit 12 (Gate L) depends on Unit 11's view-model/UI tests.
- **Tranche 3 (Units 13–14) is sequential;** Unit 13 depends on Gate L; Unit 14 depends on Gate A.
- **Unit 15 (AdminApp integration)** is **not** a dependency of any unit in this change. It is a separate future change.
- **Protected paths** are dependencies of nothing in this change; they are explicitly preserved and isolated.

## Archived

Tasks that no longer apply are moved here with `[~]` and the reason; they are not deleted.

- [~] (Unit 1c) REFACTOR: collapse the bindable options and migration scaffolding into a single `Configuration/HistoricalLoaderOptions.cs` namespace; re-run `dotnet test Rag.sln --no-build` and confirm green. — Reason: archived at the pre-11.prev-b3 registry reconciliation — the item referenced Unit 1a files (`Configuration/HistoricalLoaderOptions.cs`, `Persistence/SqliteStore.cs`) that already hold the intended structure and lay outside Unit 1c's strict edit surface (recorded as deviation #2 in `apply-progress.md`); Unit 1c closed natively as passed with the maintainer-accepted size exception, so no collapse remained actionable. <!-- sdd-owner: implementation -->
